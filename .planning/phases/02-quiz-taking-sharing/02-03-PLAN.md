---
phase: 02-quiz-taking-sharing
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - supabase/functions/start-quiz-session/index.ts
  - src/1-app/router/index.ts
  - src/5-entities/quiz-session/model.ts
  - src/5-entities/quiz-session/api.ts
  - src/4-features/quiz-taking/model/useQuizTakingStore.ts
  - src/4-features/quiz-taking/ui/GuestLoginForm.vue
  - src/4-features/quiz-taking/ui/QuizIntroScreen.vue
  - src/4-features/quiz-taking/ui/GracefulState.vue
  - src/4-features/quiz-editor/ui/NavigationSettings.vue
  - src/2-pages/QuizSharePage.vue
  - src/3-widgets/QuizTakingWidget.vue
autonomous: false
requirements: [TAKE-01, TAKE-02, TAKE-03, EXT-04]
user_setup:
  - service: supabase
    why: "start-quiz-session is a new Edge Function — must be deployed before the intro -> start flow works"
    dashboard_config:
      - task: "Deploy start-quiz-session Edge Function"
        location: "Run npx supabase functions deploy start-quiz-session"

must_haves:
  truths:
    - "A guest can open /q/:token and see the quiz title, description, cover, question count, and time limit alongside a login form"
    - "Entering correct owner-assigned credentials grants access; wrong credentials show an inline error toast without clearing fields"
    - "Clicking 'Начать' creates a quiz_session and the timer's started_at anchor; an in-progress session resumes instead of duplicating"
    - "A quiz with no questions shows the 'Тест пока не готов' graceful state, not the login form"
    - "An owner sees an 'allow_retake' toggle in the editor's NavigationSettings"
  artifacts:
    - path: "supabase/functions/start-quiz-session/index.ts"
      provides: "Session creation with D-04 resume logic and the duplicate-session guard"
    - path: "src/4-features/quiz-taking/model/useQuizTakingStore.ts"
      provides: "Guest session state machine: init, verifyAccess, startSession, sessionStatus"
      exports: ["useQuizTakingStore"]
    - path: "src/2-pages/QuizSharePage.vue"
      provides: "/q/:token public page (thin assembler)"
    - path: "src/3-widgets/QuizTakingWidget.vue"
      provides: "Widget that renders intro / graceful / active based on sessionStatus"
  key_links:
    - from: "src/1-app/router/index.ts"
      to: "src/2-pages/QuizSharePage.vue"
      via: "route /q/:token, no auth guard"
      pattern: "/q/:token"
    - from: "src/4-features/quiz-taking/model/useQuizTakingStore.ts"
      to: "verify-quiz-access / start-quiz-session"
      via: "supabase.functions.invoke"
      pattern: "functions.invoke\\('(verify-quiz-access|start-quiz-session)'"
---

<objective>
Deliver the guest entry slice: the public `/q/:token` route, the intro+login screen, the guest session store, and the `start-quiz-session` Edge Function — the path from a shared link to a started, server-anchored quiz session.

Purpose: This is the front door of the guest-taking flow. After this plan a real guest can open a link, see what the test is, authenticate, and start a session (with D-04 resume handling). The actual question-answering UI is the next slice (02-04) — this plan deliberately renders only the intro/graceful states and a placeholder for `active`.
Output: `start-quiz-session` EF, the two public routes, the `quiz-session` entity layer, `useQuizTakingStore` (init/verify/start), `GuestLoginForm` + `QuizIntroScreen` + `GracefulState`, `QuizSharePage`, the `QuizTakingWidget` shell, and the `allow_retake` toggle (EXT-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quiz-taking-sharing/02-CONTEXT.md
@.planning/phases/02-quiz-taking-sharing/02-RESEARCH.md
@.planning/phases/02-quiz-taking-sharing/02-PATTERNS.md
@.planning/phases/02-quiz-taking-sharing/02-UI-SPEC.md
@.planning/phases/02-quiz-taking-sharing/02-01-SUMMARY.md
@src/1-app/router/index.ts
@src/4-features/quiz-editor/ui/NavigationSettings.vue

<interfaces>
<!-- Read the analog files listed in each task's read_first for full patterns. -->

verify-quiz-access response (built in 02-01): { state: 'ready', guestToken, quiz, questions } | { state: 'not_ready' } | HTTP 401/410 error
start-quiz-session request: { guestToken }   response: { sessionId, started_at, resumed: boolean }

QuizSession interface (to create): id, quiz_access_id, quiz_id, started_at (ISO), finished_at (string|null), score (number|null)
SessionResult interface (to create): score, totalQuestions, percentage, label

sessionStorage key convention (RESEARCH Pattern 7): `qf_guest_{token}` -> JSON { guestToken, sessionId }

Existing router: PROTECTED_ROUTES = ['/my', '/editor']. /q/ routes must NOT be added there (public, no guard).

NavigationSettings.vue currently renders two Switch rows (allow_back, show_stop_button) bound to store.settings + store.updateSettings.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build start-quiz-session EF, the quiz-session entity layer, and the public routes</name>
  <files>supabase/functions/start-quiz-session/index.ts, src/5-entities/quiz-session/model.ts, src/5-entities/quiz-session/api.ts, src/1-app/router/index.ts</files>
  <read_first>
    - .planning/phases/02-quiz-taking-sharing/02-RESEARCH.md Pattern 1 (the fully-worked start-quiz-session) + Pattern 7 + Pitfall 3
    - .planning/phases/02-quiz-taking-sharing/02-PATTERNS.md sections for start-quiz-session, quiz-session/model.ts, quiz-session/api.ts, router/index.ts
    - src/1-app/router/index.ts (the file being modified — route array + PROTECTED_ROUTES)
    - src/5-entities/quiz/model.ts (analog for the plain-interface model)
    - supabase/functions/_shared/jwt.ts (created in 02-01 — verifyGuestToken)
  </read_first>
  <behavior>
    - start-quiz-session with an invalid/expired guestToken returns 401 and creates no session
    - start-quiz-session with a valid token and no existing open session inserts one quiz_sessions row and returns { sessionId, started_at, resumed: false }
    - start-quiz-session with a valid token and an existing open session returns that session with resumed: true (no second insert)
    - Navigating to /q/anything resolves QuizSharePage without an auth-guard redirect
  </behavior>
  <action>
    Create `supabase/functions/start-quiz-session/index.ts` — copy the RESEARCH.md Pattern 1 fully-worked example verbatim (it is the canonical reference): OPTIONS handling, `verifyGuestToken(guestToken)` -> 401 on null, service_role client, a D-04 check for an existing `quiz_sessions` row with `finished_at IS NULL` for this `quiz_access_id` (return it with `resumed: true`), otherwise INSERT a new session and return `{ sessionId, started_at, resumed: false }`. The partial unique index from migration 009 is the DB-level backstop against the double-click race (Pitfall 3).
    Create `src/5-entities/quiz-session/model.ts` — `QuizSession`, `SessionAnswer`, and `SessionResult` interfaces per the PATTERNS analog.
    Create `src/5-entities/quiz-session/api.ts` — thin `supabase.functions.invoke` wrappers, typed: `invokeVerifyAccess(token, login, password)` and `invokeStartSession(guestToken)`. All session writes go through EFs — no direct `.from('quiz_sessions')` writes (RESEARCH Pattern 9). Leave room for the answer/submit/result wrappers added in 02-04 and 02-05.
    Modify `src/1-app/router/index.ts` — add two routes `{ path: '/q/:token', component: () => import('@pages/QuizSharePage.vue') }` and `{ path: '/q/:token/result', component: () => import('@pages/QuizResultPage.vue') }`. Do NOT add `/q` to `PROTECTED_ROUTES` — guest routes are public, no auth guard. (QuizResultPage is created in 02-05; the import is lazy so the route is harmless until then — but to keep the build green, also create a minimal placeholder `QuizResultPage.vue` only if vue-tsc fails on the missing import; 02-05 replaces it.)
  </action>
  <verify>
    <automated>grep -q "/q/:token" src/1-app/router/index.ts && ! grep -q "'/q'" src/1-app/router/index.ts && test -f supabase/functions/start-quiz-session/index.ts && grep -q 'verifyGuestToken' supabase/functions/start-quiz-session/index.ts && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `start-quiz-session/index.ts` verifies the guest token, implements the existing-open-session resume branch, and inserts a new session otherwise.
    - `quiz-session/model.ts` exports `QuizSession`, `SessionAnswer`, `SessionResult`.
    - `quiz-session/api.ts` exports typed invoke wrappers for verify-access and start-session; no direct table writes.
    - `router/index.ts` has `/q/:token` and `/q/:token/result`; `PROTECTED_ROUTES` is unchanged.
    - `npx vue-tsc --noEmit` passes.
  </acceptance_criteria>
  <done>start-quiz-session EF, quiz-session entity layer, and public guest routes implemented; session resume logic in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the useQuizTakingStore entry actions</name>
  <files>src/4-features/quiz-taking/model/useQuizTakingStore.ts</files>
  <read_first>
    - .planning/phases/02-quiz-taking-sharing/02-RESEARCH.md Pattern 7 (session resume/init) + Pattern 9 + the D-04 state machine in CONTEXT.md
    - .planning/phases/02-quiz-taking-sharing/02-PATTERNS.md "useQuizTakingStore.ts" section (store structure, error handling)
    - src/4-features/quiz-editor/model/useQuizEditorStore.ts (analog store — defineStore composition, refs, try/catch + toast.error)
    - src/5-entities/quiz-session/api.ts (created in Task 1 — invoke wrappers)
  </read_first>
  <behavior>
    - init(token) with nothing in sessionStorage sets sessionStatus = 'idle'
    - verifyAccess(login, password) on success stores guestToken in sessionStorage under qf_guest_{token} and sets sessionStatus = 'intro'
    - verifyAccess on a 'not_ready' response sets sessionStatus = 'not_ready'
    - verifyAccess on wrong credentials throws (caller shows the toast) and does NOT change sessionStatus away from 'idle'
    - startSession() invokes start-quiz-session, persists sessionId to sessionStorage, sets sessionStatus = 'active', and is guarded against double-invocation by an isStarting flag
  </behavior>
  <action>
    Create `src/4-features/quiz-taking/model/useQuizTakingStore.ts` as `defineStore('quiz-taking', ...)` composition store. This plan implements ONLY the entry portion of the state machine (answering, timer, submit come in 02-04 / 02-05) — but declare all state refs now so later plans only add actions:
    State refs: `sessionStatus` (`'idle' | 'intro' | 'active' | 'finished' | 'not_ready' | 'invalid'`), `token`, `guestToken`, `sessionId`, `quiz`, `questions`, `answers` (`Record<string,string[]>`), `currentQuestionIndex`, `isLoading`, `isStarting`, plus timer refs (`startedAt`, `timeLimitSec`, `timeRemainingSeconds`) and `result` — leave the timer/answer/submit refs unused for now.
    Actions for this plan:
    - `init(token)` — store the token; read `sessionStorage.getItem('qf_guest_' + token)`. If absent -> `sessionStatus = 'idle'`. If present -> invoke `start-quiz-session` with the stored guestToken to drive the D-04 state machine (resume -> 'active'; the finished/expired branches are completed in 02-05, so for now: if `resumed` -> set 'active' and stash sessionId; if the call 401s -> set 'invalid'/'idle' and clear sessionStorage).
    - `verifyAccess(login, password)` — set `isLoading`, invoke `verify-quiz-access`. On `state: 'ready'`: stash `guestToken`, `quiz`, `questions`, write `{ guestToken }` to `sessionStorage`, `sessionStatus = 'intro'`. On `state: 'not_ready'`: `sessionStatus = 'not_ready'`. On a 401/410 error: rethrow so `GuestLoginForm` shows the toast — do not mutate `sessionStatus`. Wrap in try/catch per the project pattern.
    - `startSession()` — D-02: guarded by `isStarting`; invoke `start-quiz-session` with `guestToken`; merge `{ guestToken, sessionId }` into `sessionStorage`; set `startedAt`/`timeLimitSec` from the started session + `quiz.time_limit_sec`; `sessionStatus = 'active'`.
    - `cleanup()` — placeholder that 02-04 fills (timer teardown); define it now so the widget can call it.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit && npx eslint src/4-features/quiz-taking/model</automated>
  </verify>
  <acceptance_criteria>
    - `useQuizTakingStore` exports a store with all listed state refs and the actions `init`, `verifyAccess`, `startSession`, `cleanup`.
    - `verifyAccess` writes `qf_guest_{token}` to `sessionStorage` on success and rethrows on a credential error.
    - `startSession` is guarded against double-invocation.
    - Imports are `@entities`/`@shared` only — no feature-to-feature import.
    - `npx vue-tsc --noEmit` and eslint pass.
  </acceptance_criteria>
  <done>useQuizTakingStore entry state machine (init/verify/start) implemented; all later-plan refs declared.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The guest intro UI (`GuestLoginForm`, `QuizIntroScreen`, `GracefulState`), the `QuizSharePage`, the `QuizTakingWidget` shell, and the `allow_retake` toggle in the editor. Combined with Tasks 1-2 this completes the guest entry slice.
  </what-built>
  <how-to-verify>
    First the executor completes the UI (this checkpoint follows automated work). Build per UI-SPEC sections 1 and 6 and the analogs in 02-PATTERNS.md:
    - `src/4-features/quiz-taking/ui/GuestLoginForm.vue` — login + password Inputs and an orange full-width "Начать" Button with a Loader2 loading state; calls `store.verifyAccess`, on a thrown credential error shows `toast.error("Неверный логин или пароль.")` and does NOT clear the fields (analog: `auth/ui/LoginForm.vue`).
    - `src/4-features/quiz-taking/ui/QuizIntroScreen.vue` — the centered `bg-neutral-900 rounded-2xl` card: optional cover image, quiz title, meta row ("N вопросов · M мин" using `formatDuration` from `@shared/lib/format`), optional description, a divider, then the embedded `GuestLoginForm` when `sessionStatus==='idle'`, OR — when `sessionStatus==='intro'` — the same card with an orange "Начать" button calling `store.startSession`.
    - `src/4-features/quiz-taking/ui/GracefulState.vue` — a reusable centered card taking icon/heading/body props; renders the UI-SPEC section 6 states ("Тест пока не готов", "Ссылка недействительна", etc.).
    - `src/2-pages/QuizSharePage.vue` — a thin (<=80 line) page that mounts `QuizTakingWidget`; no AppHeader.
    - `src/3-widgets/QuizTakingWidget.vue` — reads `route.params.token`, calls `store.init(token)` on mount and `store.cleanup()` on unmount; renders `QuizIntroScreen` for `idle`/`intro`, `GracefulState` for `not_ready`/`invalid`, and a temporary placeholder for `active` (02-04 replaces it).
    - Modify `src/4-features/quiz-editor/ui/NavigationSettings.vue` — add a third Switch row "Разрешить повторное прохождение" bound to `store.settings.allow_retake` / `store.updateSettings({ allow_retake: $event })` (EXT-04, D-03), exactly matching the existing two rows.
    Then manually verify (requires `npx supabase functions deploy start-quiz-session` and an access link created via Plan 02-02):
    1. Open `/q/{token}` for a real link — confirm the intro card shows title, description, cover, "N вопросов · M мин", and the login form. No AppHeader.
    2. Enter wrong credentials — confirm the "Неверный логин или пароль." toast and that the fields keep their values.
    3. Enter correct credentials — confirm the screen advances to the intro state with a "Начать" button.
    4. Click "Начать" — confirm a `quiz_sessions` row is created (Supabase Studio) and the widget switches to the `active` placeholder.
    5. Reload `/q/{token}` — confirm it resumes the in-progress session (no second `quiz_sessions` row, no duplicate-key error).
    6. Open a quiz with zero questions via its link — confirm the "Тест пока не готов" graceful state.
    7. In the quiz editor, confirm the "Разрешить повторное прохождение" toggle appears and persists.
  </how-to-verify>
  <resume-signal>Type "approved" once the intro screen, credential flow, session start/resume, the no-questions state, and the allow_retake toggle all work. Describe any visual or behavior issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guest browser -> start-quiz-session EF | Untrusted guestToken crosses here; verified before any DB write |
| sessionStorage <-> guest browser | guestToken stored client-side; opaque to the client, only the EF can verify it |
| Public /q/:token route | No auth guard — anyone with the URL reaches the page; credential check is the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Spoofing | forged guestToken in start-quiz-session call | mitigate | verifyGuestToken (jose, HS256) rejects bad signatures and expired tokens; 401 before any DB access |
| T-02-13 | Tampering | double-click / re-mount creates duplicate sessions | mitigate | isStarting store guard + start-quiz-session resume branch + migration 009 partial unique index on quiz_sessions(quiz_access_id) WHERE finished_at IS NULL |
| T-02-14 | Spoofing | forged sessionId injected by the client | mitigate | start-quiz-session derives the session from the token's quiz_access_id, never trusts a client-supplied sessionId |
| T-02-15 | Information Disclosure | guest token stolen and replayed | accept | 1h JWT TTL bounds the window; sessionStorage clears on tab close; quiz-taking is low-value data — short TTL is the proportionate control |
| T-02-16 | Denial of Service | guest token replay after expiry | mitigate | jwtVerify enforces the exp claim automatically; expired token -> 401 -> store routes to 'invalid'/session-expired graceful state |
</threat_model>

<verification>
- `start-quiz-session` deployed; an invalid token 401s; a valid token creates or resumes exactly one session.
- `/q/:token` is reachable without authentication and renders the intro screen.
- Wrong credentials show a toast and keep the form populated; correct credentials advance to the "Начать" state.
- A zero-question quiz shows the graceful "Тест пока не готов" state.
- The `allow_retake` toggle appears in the editor and persists to `quizzes.settings`.
- `npx vue-tsc --noEmit`, `npx eslint`, and steiger all pass.
</verification>

<success_criteria>
- A guest can open a token link, see the quiz intro, authenticate, and start a server-anchored session (TAKE-01, TAKE-02, TAKE-03).
- Re-opening the link resumes an in-progress session without creating a duplicate (D-04 resume branch).
- The owner can configure multiple attempts via the `allow_retake` toggle (EXT-04, D-03).
</success_criteria>

<output>
Create `.planning/phases/02-quiz-taking-sharing/02-03-SUMMARY.md` when done.
</output>
