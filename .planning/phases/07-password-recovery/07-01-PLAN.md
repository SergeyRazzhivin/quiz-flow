---
phase: 07-password-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/4-features/auth/model/useAuthStore.ts
  - src/4-features/auth/model/useAuthStore.test.ts
  - src/2-pages/ForgotPasswordPage.vue
  - src/2-pages/ResetPasswordPage.vue
  - src/4-features/auth/ui/LoginForm.vue
  - src/1-app/router/index.ts
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05
  - AUTH-06
tags:
  - auth
  - password-recovery
  - supabase
  - vue
  - pinia
must_haves:
  truths:
    - "From the sign-in form (LoginForm) the owner can click 'Забыли пароль?' and arrive on /forgot-password"
    - "Submitting any email on /forgot-password (registered or not) shows the same generic Russian success message — the UI never confirms whether the email exists"
    - "After clicking the recovery email link, /reset-password establishes a recovery session via the Supabase PASSWORD_RECOVERY auth event and accepting a new password redirects the owner to /my"
    - "When /reset-password is opened without a valid recovery session (expired/reused/tampered link) within ~1.5 s, it shows a Russian-language error card with a link back to /forgot-password and never silently signs the user in"
  artifacts:
    - path: "src/4-features/auth/model/useAuthStore.ts"
      provides: "requestPasswordReset(email) and updatePassword(newPassword) methods exported from the existing auth store"
      contains: "requestPasswordReset"
    - path: "src/4-features/auth/model/useAuthStore.test.ts"
      provides: "Vitest unit tests for both new store methods (success path + Supabase error path, including the 'User not found' swallow-and-resolve case)"
      contains: "requestPasswordReset"
    - path: "src/2-pages/ForgotPasswordPage.vue"
      provides: "Public /forgot-password page (single email input + submit + generic success state)"
      min_lines: 30
    - path: "src/2-pages/ResetPasswordPage.vue"
      provides: "Public /reset-password page (PASSWORD_RECOVERY listener + new-password/confirm-password form + error card on stale link)"
      min_lines: 50
    - path: "src/4-features/auth/ui/LoginForm.vue"
      provides: "'Забыли пароль?' RouterLink to /forgot-password rendered under the password field"
      contains: "/forgot-password"
    - path: "src/1-app/router/index.ts"
      provides: "/forgot-password and /reset-password routes (public, no requiresAuth meta) registered with the router"
      contains: "/forgot-password"
  key_links:
    - from: "src/4-features/auth/model/useAuthStore.ts"
      to: "supabase.auth.resetPasswordForEmail"
      via: "direct call inside requestPasswordReset"
      pattern: "resetPasswordForEmail"
    - from: "src/4-features/auth/model/useAuthStore.ts"
      to: "supabase.auth.updateUser"
      via: "direct call inside updatePassword"
      pattern: "updateUser"
    - from: "src/2-pages/ResetPasswordPage.vue"
      to: "supabase.auth.onAuthStateChange"
      via: "PASSWORD_RECOVERY event listener established on mount"
      pattern: "PASSWORD_RECOVERY"
    - from: "src/2-pages/ForgotPasswordPage.vue"
      to: "src/4-features/auth/model/useAuthStore.ts (requestPasswordReset)"
      via: "store call with redirectTo computed from window.location.origin + import.meta.env.BASE_URL + 'reset-password'"
      pattern: "BASE_URL"
    - from: "src/4-features/auth/ui/LoginForm.vue"
      to: "/forgot-password route"
      via: "RouterLink under the password input"
      pattern: "RouterLink"
    - from: "src/1-app/router/index.ts"
      to: "ForgotPasswordPage.vue + ResetPasswordPage.vue"
      via: "lazy-loaded route entries without meta.requiresAuth"
      pattern: "ForgotPasswordPage|ResetPasswordPage"
---

<objective>
Ship the full owner-side password-recovery slice (AUTH-04 / AUTH-05 / AUTH-06) in a single vertical slice: extend the existing Pinia auth store with `requestPasswordReset(email)` and `updatePassword(newPassword)`, add the matching unit tests, build the two public pages `/forgot-password` and `/reset-password`, surface the entry-point link inside the existing sign-in form, and register the two new routes.

Purpose: An owner who has forgotten their password must be able to recover it through the standard Supabase email-link flow without leaking whether a given email is registered, with the UI matching the existing AuthPage visual language and the existing FSD layer rules from CLAUDE.md.

Output:
- 2 new methods on `useAuthStore` (+ tests)
- 2 new public pages
- 1 modified existing form (entry-point link)
- 2 new public routes
- Plan SUMMARY in `.planning/phases/07-password-recovery/07-01-SUMMARY.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-password-recovery/07-CONTEXT.md
@CLAUDE.md

@src/4-features/auth/model/useAuthStore.ts
@src/4-features/auth/model/useAuthStore.test.ts
@src/4-features/auth/ui/LoginForm.vue
@src/4-features/auth/ui/RegisterForm.vue
@src/2-pages/AuthPage.vue
@src/1-app/router/index.ts
@src/6-shared/api/supabase.ts
@vite.config.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->

From src/6-shared/api/supabase.ts:
- Named export `supabase` — typed `SupabaseClient<Database>` from `@supabase/supabase-js`, instantiated with `SUPABASE_URL` + `SUPABASE_ANON_KEY` from `@shared/config/env`. New code MUST import via `import { supabase } from '@shared/api/supabase'`.

From src/4-features/auth/model/useAuthStore.ts (existing exports — extend, do not replace):
- `useAuthStore` — Pinia setup store returning `{ user, isLoading, init, login, register, logout }`.
- Existing pattern for write methods: `async function login(email, password) { const { error } = await supabase.auth.signInWithPassword({ email, password }); if (error) throw error }`.
- Existing pattern: store methods never call `toast.*`; they throw on Supabase error, and the calling UI shows a toast (see LoginForm.vue onSubmit catch block).

From src/4-features/auth/ui/LoginForm.vue:
- vee-validate + @vee-validate/zod + `z` schema pattern; `useForm({ validationSchema: toTypedSchema(schema) })`.
- Layout: `<form class="mt-4 space-y-4" @submit="onSubmit">`, labelled `<input>` with the existing `border-neutral-700 / focus:ring-orange-500` tokens, submit `<button class="w-full cursor-pointer rounded-lg bg-orange-500 ... hover:bg-orange-600 disabled:cursor-not-allowed disabled:opacity-50">`. ForgotPassword/ResetPassword forms MUST reuse these exact token classes (NOT the violet→indigo gradient mentioned offhand in CONTEXT.md — the canonical AuthPage uses `bg-orange-500`; reuse it for visual consistency, which is what CONTEXT.md's "match the existing AuthPage" clause actually requires).

From src/1-app/router/index.ts:
- `createRouter({ history: createWebHistory(import.meta.env.BASE_URL), routes: [...] })`.
- Route entries are flat objects: `{ path, component: () => import('@pages/X.vue'), meta?: { requiresAuth: true } }`.
- The `router.beforeEach` guard treats a missing `meta.requiresAuth` as public — recovery routes MUST NOT set `requiresAuth: true`.

From vite.config.ts:
- `base: process.env.VITE_BASE ?? '/'` — at runtime `import.meta.env.BASE_URL` returns `'/quiz-flow/'` on GitHub Pages and `'/'` on local dev. The recovery `redirectTo` URL MUST be derived as `window.location.origin + import.meta.env.BASE_URL + 'reset-password'` so both environments work (CONTEXT.md LOCKED decision).

From src/4-features/auth/model/useAuthStore.test.ts:
- `vi.mock('@shared/api/supabase', () => ({ supabase: { auth: { ... mocked methods ... } } }))` factory pattern, then `vi.mocked(supabase.auth.X).mockResolvedValue(...)`.
- `setActivePinia(createPinia())` + `vi.clearAllMocks()` in `beforeEach`.
- New tests MUST register `resetPasswordForEmail` and `updateUser` mocks in the same factory block (the factory is hoisted by Vitest — add the two new keys alongside `signInWithPassword`/`signUp`/`signOut`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add requestPasswordReset + updatePassword to useAuthStore (with tests)</name>
  <files>src/4-features/auth/model/useAuthStore.ts, src/4-features/auth/model/useAuthStore.test.ts</files>
  <read_first>
    - src/4-features/auth/model/useAuthStore.ts (current `login` / `register` / `logout` shape — new methods MUST mirror this exactly)
    - src/4-features/auth/model/useAuthStore.test.ts (existing vi.mock factory pattern — new tests extend the same `supabase.auth` mock object)
    - .planning/phases/07-password-recovery/07-CONTEXT.md sections `<decisions>` (Supabase Flow LOCKED, Email Security LOCKED) and `<specifics>` (the `requestPasswordReset` MUST NOT throw on User-not-found, returns void in all cases)
    - vite.config.ts (confirms `base` is read from `VITE_BASE` and surfaced via `import.meta.env.BASE_URL`)
  </read_first>
  <behavior>
    requestPasswordReset(email):
    - Test 1: Calls `supabase.auth.resetPasswordForEmail` with the email argument and a `redirectTo` option equal to `window.location.origin + import.meta.env.BASE_URL + 'reset-password'`. Resolves with `undefined` on success.
    - Test 2: When Supabase returns `error: { message: 'User not found', status: 400 }`, the method STILL resolves with `undefined` (does NOT throw). This is the LOCKED no-email-enumeration decision from 07-CONTEXT.md (`<decisions>` Email Security): the UI must never learn whether the email exists. Errors MAY be logged with `console.warn` for local debugging but MUST NOT throw.
    - Test 3: When Supabase returns any other error (e.g. network/`AuthApiError` with non-"User not found" message), the method ALSO resolves quietly — the locked decision is "generic success regardless". Document the rationale with an inline comment referencing CONTEXT.md Email Security LOCKED.

    updatePassword(newPassword):
    - Test 1: Calls `supabase.auth.updateUser({ password: newPassword })`. Resolves on success.
    - Test 2: When Supabase returns `error: { message: 'New password should be different from the old password', status: 422 }`, the method throws that error so the UI can surface it (this method is allowed to throw — distinct from `requestPasswordReset` because the user is mid-recovery-session and an error here is actionable, not enumeration-leaking).
  </behavior>
  <action>
    Extend `useAuthStore` with two new functions per the LOCKED decisions in 07-CONTEXT.md:

    1. `async function requestPasswordReset(email: string): Promise<void>` — compute `const redirectTo = window.location.origin + import.meta.env.BASE_URL + 'reset-password'`, then `await supabase.auth.resetPasswordForEmail(email, { redirectTo })`. If the call returns an error, swallow it (optionally `console.warn('[auth] resetPasswordForEmail error', error.message)` for debugging) and resolve normally. Add an inline comment: `// CONTEXT.md Email Security LOCKED: never reveal whether an email is registered — swallow all Supabase errors so the UI always shows the generic success message.` Export from the returned object.

    2. `async function updatePassword(newPassword: string): Promise<void>` — `const { error } = await supabase.auth.updateUser({ password: newPassword })`. If `error`, `throw error`. Export from the returned object.

    3. In `useAuthStore.test.ts`: extend the existing hoisted `vi.mock('@shared/api/supabase', ...)` factory by adding two new mock fns: `mockResetPasswordForEmail = vi.fn()` and `mockUpdateUser = vi.fn()`, wired into `supabase.auth.resetPasswordForEmail` and `supabase.auth.updateUser` respectively (mirror the existing structure exactly — do NOT introduce a separate mock file).

    4. Add the test cases described in `<behavior>`. For the `redirectTo` assertion, stub `window.location` via happy-dom's default `http://localhost/` origin (no extra config needed — `import.meta.env.BASE_URL` will resolve to `'/'` in the vitest config). The expected `redirectTo` value in the assertion is `'http://localhost/reset-password'`.

    5. Do NOT alter the existing `init` / `login` / `register` / `logout` methods, exports, or test cases.
  </action>
  <verify>
    <automated>npx vitest run src/4-features/auth/model/useAuthStore.test.ts</automated>
  </verify>
  <done>
    - `useAuthStore` exports `requestPasswordReset` and `updatePassword` alongside the existing methods (returned-object keys match).
    - All existing tests still pass.
    - 5 new test cases pass (3 for requestPasswordReset, 2 for updatePassword).
    - `grep -n "resetPasswordForEmail\|updateUser" src/4-features/auth/model/useAuthStore.ts` shows both Supabase API calls present.
    - `grep -n "BASE_URL.*reset-password" src/4-features/auth/model/useAuthStore.ts` confirms the redirectTo derivation matches the LOCKED CONTEXT.md formula.
    - `grep -n "Email Security LOCKED" src/4-features/auth/model/useAuthStore.ts` confirms the rationale comment is present.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create ForgotPasswordPage with generic-success-only UX</name>
  <files>src/2-pages/ForgotPasswordPage.vue</files>
  <read_first>
    - src/2-pages/AuthPage.vue (wrapper layout: `min-h-screen`, centred `max-w-md` card, heading + sub-copy block)
    - src/4-features/auth/ui/LoginForm.vue (vee-validate + zod pattern, input token classes, submit button token classes — REUSE these exact class strings verbatim)
    - src/4-features/auth/model/useAuthStore.ts (after Task 1 completes — calling `authStore.requestPasswordReset(email)`)
    - .planning/phases/07-password-recovery/07-CONTEXT.md sections `<decisions>` UX Surface LOCKED + Email Security LOCKED, and `<specifics>` (exact Russian copy strings to seed)
    - CLAUDE.md (FSD: `2-pages` is a thin assembler ≤80 lines; cursor-pointer on every clickable)
  </read_first>
  <action>
    Create `src/2-pages/ForgotPasswordPage.vue` as a thin page (≤80 lines) that mirrors the AuthPage shell:

    1. `<script setup lang="ts">`: import `ref` from vue, `useForm` from vee-validate, `toTypedSchema` from `@vee-validate/zod`, `z` from zod, `useAuthStore` from `@features/auth/model/useAuthStore`, and `RouterLink` (auto-available, no explicit import needed if registered globally — otherwise import from `vue-router`).
    2. Schema: `z.object({ email: z.string().email('Некорректный email') })`. Form via `useForm({ validationSchema: toTypedSchema(schema) })`, `[email, emailAttrs] = defineField('email')`.
    3. Local `const isSubmitted = ref(false)` — flips to `true` after the store call resolves; toggles the template between form view and generic-success view.
    4. `onSubmit = handleSubmit(async (values) => { await authStore.requestPasswordReset(values.email); isSubmitted.value = true })`. Per LOCKED CONTEXT.md Email Security: do NOT wrap in try/catch with a toast — the store already swallows errors and resolves quietly. The form always succeeds from the UI's perspective.
    5. Template structure (mirror AuthPage's outer `flex min-h-screen items-center justify-center` wrapper and `w-full max-w-md p-6` inner card):
       - Heading: `<h1 class="text-2xl font-bold text-neutral-50">Восстановление пароля</h1>`
       - Sub-copy when `!isSubmitted`: `<p class="mt-2 text-sm leading-relaxed text-neutral-400">Введите email — отправим ссылку для сброса пароля.</p>`
       - Form (when `!isSubmitted`): single email input + submit button. Reuse the EXACT class strings from `LoginForm.vue` for label, input, and button (`border-neutral-700`, `focus:ring-orange-500`, `bg-orange-500 hover:bg-orange-600 cursor-pointer disabled:cursor-not-allowed disabled:opacity-50`). Submit button label: `'Отправить ссылку'` (or `'Отправка...'` while `isSubmitting`).
       - Success state (when `isSubmitted`): replace form with a card showing `<p class="mt-4 text-sm text-neutral-200">Если такой email зарегистрирован, мы отправили на него ссылку для сброса пароля.</p>` followed by `<RouterLink to="/auth" class="mt-4 inline-block cursor-pointer text-sm text-orange-400 hover:text-orange-300">Вернуться ко входу</RouterLink>`.
       - Below the form (always visible, also when `!isSubmitted`): `<RouterLink to="/auth" class="mt-4 inline-block cursor-pointer text-sm text-neutral-400 hover:text-neutral-200">Назад ко входу</RouterLink>`.
    6. Do NOT introduce widget-layer composition; this page is a 2-pages assembler that owns the form inline (the form is small enough — per CONTEXT.md FSD Layout LOCKED, the widget layer is NOT involved).
    7. Every clickable element MUST have `cursor-pointer` per CLAUDE.md / Memory rule.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit -p tsconfig.app.json 2>&1 | grep -E "ForgotPasswordPage|error" || echo "ok"</automated>
  </verify>
  <done>
    - File exists at `src/2-pages/ForgotPasswordPage.vue`, ≤80 lines.
    - `grep -c "Восстановление пароля" src/2-pages/ForgotPasswordPage.vue` returns ≥1.
    - `grep -c "Если такой email зарегистрирован" src/2-pages/ForgotPasswordPage.vue` returns ≥1 (the LOCKED generic-success copy).
    - `grep -c "requestPasswordReset" src/2-pages/ForgotPasswordPage.vue` returns ≥1.
    - `grep -c "try" src/2-pages/ForgotPasswordPage.vue` returns 0 — there is NO try/catch (LOCKED: store swallows errors, UI always shows success).
    - `grep -c "cursor-pointer" src/2-pages/ForgotPasswordPage.vue` returns ≥2 (submit button + back link).
    - `vue-tsc --noEmit` reports no errors for this file.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create ResetPasswordPage with PASSWORD_RECOVERY listener + stale-link error state</name>
  <files>src/2-pages/ResetPasswordPage.vue</files>
  <read_first>
    - src/2-pages/AuthPage.vue (outer shell — reuse the same `flex min-h-screen ... max-w-md p-6` wrapper)
    - src/4-features/auth/ui/LoginForm.vue (vee-validate + zod form scaffold + token classes to reuse)
    - src/6-shared/api/supabase.ts (the `supabase` named export)
    - src/4-features/auth/model/useAuthStore.ts (after Task 1: `updatePassword`; also existing `init()` for refreshing user state after recovery)
    - .planning/phases/07-password-recovery/07-CONTEXT.md sections `<decisions>` Supabase Flow LOCKED + Failure States LOCKED, and `<specifics>` (the 1.5 s timeout window + exact Russian error copy)
  </read_first>
  <action>
    Create `src/2-pages/ResetPasswordPage.vue` (~50–80 lines) implementing the LOCKED recovery-session flow from CONTEXT.md:

    1. `<script setup lang="ts">`: import `ref`, `onMounted`, `onBeforeUnmount` from vue; `useRouter` from vue-router; `useForm` from vee-validate; `toTypedSchema` from `@vee-validate/zod`; `z` from zod; `toast` from `vue-sonner`; `useAuthStore` from `@features/auth/model/useAuthStore`; `supabase` from `@shared/api/supabase`.
    2. Recovery-session state machine: `const recoveryState = ref<'checking' | 'ready' | 'invalid'>('checking')`. Render the form only when `recoveryState === 'ready'`; render an error card when `recoveryState === 'invalid'`; render a small spinner/placeholder when `recoveryState === 'checking'`.
    3. On mount, subscribe to Supabase recovery events per LOCKED decision (`PASSWORD_RECOVERY` event listener + 1.5 s timeout):
       - `const { data: { subscription } } = supabase.auth.onAuthStateChange((event, session) => { if (event === 'PASSWORD_RECOVERY' && session) { recoveryState.value = 'ready'; clearTimeout(timeoutHandle) } })`.
       - Also probe immediately: `const { data: { session } } = await supabase.auth.getSession()`. If a recovery-scoped session already exists (i.e. the event fired before mount), set `recoveryState.value = 'ready'`.
       - Set `const timeoutHandle = window.setTimeout(() => { if (recoveryState.value === 'checking') recoveryState.value = 'invalid' }, 1500)`.
       - In `onBeforeUnmount`, call `subscription.unsubscribe()` and `clearTimeout(timeoutHandle)`.
    4. Form schema: `z.object({ password: z.string().min(6, 'Минимум 6 символов'), confirm: z.string().min(6, 'Минимум 6 символов') }).refine((v) => v.password === v.confirm, { path: ['confirm'], message: 'Пароли не совпадают' })`. Use `defineField` for both, mirror LoginForm input/label classes.
    5. `onSubmit = handleSubmit(async (values) => { try { await authStore.updatePassword(values.password); await authStore.init(); toast.success('Пароль обновлён'); router.push('/my') } catch (err) { toast.error(err instanceof Error ? err.message : 'Не удалось обновить пароль') } })`. Per CONTEXT.md `<specifics>`: explicitly call `authStore.init()` (or wait for `SIGNED_IN`) so `authStore.user` is refreshed before navigating to `/my`.
    6. Template structure:
       - Outer wrapper: identical to AuthPage / ForgotPasswordPage (`flex min-h-screen items-center justify-center`, `w-full max-w-md p-6`).
       - Heading (always): `<h1 class="text-2xl font-bold text-neutral-50">Новый пароль</h1>`.
       - `v-if="recoveryState === 'checking'"`: small loader text `<p class="mt-4 text-sm text-neutral-400">Проверка ссылки...</p>`.
       - `v-else-if="recoveryState === 'invalid'"`: error card with `<p class="mt-4 text-sm text-red-400">Ссылка недействительна или истекла. Запросите новую ссылку.</p>` + `<RouterLink to="/forgot-password" class="mt-4 inline-block cursor-pointer text-sm text-orange-400 hover:text-orange-300">Запросить новую ссылку</RouterLink>`.
       - `v-else` (`ready`): hint `<p class="mt-2 text-sm leading-relaxed text-neutral-400">Введите новый пароль и подтверждение.</p>` and the two-input form with reused LoginForm token classes. Submit button label: `'Сохранить пароль'` (or `'Сохранение...'` while `isSubmitting`).
    7. `cursor-pointer` on every clickable (submit button, error-card RouterLink).
    8. NEVER render the form while `recoveryState !== 'ready'` (LOCKED Failure States: never silently sign user in with stale session).
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit -p tsconfig.app.json 2>&1 | grep -E "ResetPasswordPage|error" || echo "ok"</automated>
  </verify>
  <done>
    - File exists at `src/2-pages/ResetPasswordPage.vue`.
    - `grep -c "PASSWORD_RECOVERY" src/2-pages/ResetPasswordPage.vue` returns ≥1.
    - `grep -c "1500" src/2-pages/ResetPasswordPage.vue` returns ≥1 (LOCKED 1.5 s timeout).
    - `grep -c "updatePassword" src/2-pages/ResetPasswordPage.vue` returns ≥1.
    - `grep -c "Ссылка недействительна или истекла" src/2-pages/ResetPasswordPage.vue` returns ≥1 (LOCKED error copy).
    - `grep -c "router.push('/my')" src/2-pages/ResetPasswordPage.vue` returns ≥1.
    - `grep -c "authStore.init" src/2-pages/ResetPasswordPage.vue` returns ≥1 (CONTEXT specifics: refresh user before navigating).
    - `grep -c "unsubscribe" src/2-pages/ResetPasswordPage.vue` returns ≥1 (subscription cleanup in onBeforeUnmount).
    - `grep -c "cursor-pointer" src/2-pages/ResetPasswordPage.vue` returns ≥2.
    - vue-tsc reports no errors for this file.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add 'Забыли пароль?' link to LoginForm and register the two new public routes</name>
  <files>src/4-features/auth/ui/LoginForm.vue, src/1-app/router/index.ts</files>
  <read_first>
    - src/4-features/auth/ui/LoginForm.vue (the existing sign-in form — link is added under the password input, BEFORE the submit button)
    - src/1-app/router/index.ts (existing flat routes array — new entries go alongside `/auth`, NO `meta.requiresAuth`)
    - .planning/phases/07-password-recovery/07-CONTEXT.md sections `<decisions>` UX Surface LOCKED ("Забыли пароль?" link in sign-in tab ONLY, NOT sign-up tab) + FSD Layout LOCKED (routes file is the only router change)
  </read_first>
  <action>
    Two edits, both small:

    1. In `src/4-features/auth/ui/LoginForm.vue`:
       - Import `RouterLink` from `vue-router` at the top of `<script setup>` (or rely on global registration if the project already does so — check existing pages; if `RouterLink` is used unimported elsewhere, no import needed).
       - Inside the `<form>` template, immediately AFTER the password `<div>` block and BEFORE the submit `<button>`, add:
         `<div class="text-right"><RouterLink to="/forgot-password" class="cursor-pointer text-sm text-orange-400 hover:text-orange-300">Забыли пароль?</RouterLink></div>`
       - Do NOT modify `RegisterForm.vue` — the link belongs to the sign-in tab ONLY per LOCKED UX Surface decision.

    2. In `src/1-app/router/index.ts`, add two new entries to the `routes` array, placed immediately AFTER the existing `{ path: '/auth', ... }` entry (so all public auth-related routes group together):
       - `{ path: '/forgot-password', component: () => import('@pages/ForgotPasswordPage.vue') }`
       - `{ path: '/reset-password',  component: () => import('@pages/ResetPasswordPage.vue') }`
       - NEITHER route sets `meta: { requiresAuth: true }` — both are public per LOCKED UX Surface decision. Do NOT modify the `router.beforeEach` guard.
  </action>
  <verify>
    <automated>npx vitest run src/4-features/auth/model/useAuthStore.test.ts && npx vue-tsc --noEmit -p tsconfig.app.json 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `grep -c "Забыли пароль" src/4-features/auth/ui/LoginForm.vue` returns 1.
    - `grep -c "/forgot-password" src/4-features/auth/ui/LoginForm.vue` returns 1.
    - `grep -c "Забыли пароль" src/4-features/auth/ui/RegisterForm.vue` returns 0 (link is sign-in-only per LOCKED).
    - `grep -c "/forgot-password" src/1-app/router/index.ts` returns 1.
    - `grep -c "/reset-password" src/1-app/router/index.ts` returns 1.
    - `grep -c "requiresAuth" src/1-app/router/index.ts` shows the count is unchanged from before (no new requiresAuth meta added — both new routes are public).
    - vue-tsc reports no new errors.
    - All existing vitest specs still pass.
  </done>
</task>

</tasks>

<verification>

Full phase verification (run at the end):

1. `npx vitest run` — entire test suite green, including the 5 new auth-store cases.
2. `npx vue-tsc --noEmit -p tsconfig.app.json` — no type errors.
3. `npx steiger src` — no new FSD layer violations (2-pages does not reach into 3-widgets; recovery forms live inline in the page or under 4-features/auth as private; no feature-to-feature import).
4. `npm run build` — production build succeeds and emits `dist/assets/ForgotPasswordPage*.js` and `dist/assets/ResetPasswordPage*.js` chunks (lazy-loaded routes).
5. Manual smoke (developer): `npm run dev`, open `http://localhost:5173/auth`, click "Забыли пароль?", confirm navigation to `/forgot-password`. Submit any email — UI shows the generic success message. Open `/reset-password` directly (no recovery link) — UI shows the "Ссылка недействительна или истекла" error card after ~1.5 s, with a working link back to `/forgot-password`.
6. Manual smoke (full round-trip, requires Supabase Dashboard URL Configuration `https://sergeyrazzhivin.github.io/quiz-flow/**` allow-list still in place per CONTEXT Out-of-band Setup LOCKED): submit a real registered email, click the recovery email link, land on `/reset-password`, set a new password, confirm redirect to `/my` and that the new password works on next sign-in.

</verification>

<success_criteria>

Plan succeeds when:

1. `useAuthStore` exports `requestPasswordReset` and `updatePassword`; the existing `login` / `register` / `logout` / `init` API is unchanged.
2. `useAuthStore.test.ts` has 5 new passing cases covering: success path of `requestPasswordReset`; the "User not found" Supabase error is swallowed (no throw); other Supabase errors are also swallowed; success path of `updatePassword`; error path of `updatePassword` throws.
3. `/forgot-password` renders, submits via the store, and shows the LOCKED Russian generic success message regardless of email registration status.
4. `/reset-password` renders only the form when a `PASSWORD_RECOVERY` event has been received (or `getSession()` returns a recovery session) within 1.5 s of mount; otherwise it renders the LOCKED stale-link error card with a CTA back to `/forgot-password`. A successful `updatePassword` call refreshes the auth store and navigates to `/my`.
5. `LoginForm.vue` shows "Забыли пароль?" under the password input (sign-in tab only). `RegisterForm.vue` is unchanged.
6. Router has two new public routes (`/forgot-password`, `/reset-password`) with NO `meta.requiresAuth`. The `beforeEach` guard is untouched.
7. All four Phase 7 must-have truths from ROADMAP.md and this plan's `must_haves.truths` are demonstrably achievable.

</success_criteria>

<output>
Create `.planning/phases/07-password-recovery/07-01-SUMMARY.md` when done, following the standard SUMMARY template (covered_requirements, artifacts, key decisions, follow-ups).
</output>
