---
phase: 01-foundation-auth-and-quiz-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - index.html
  - eslint.config.ts
  - steiger.config.ts
  - .env.example
  - .gitignore
  - .env
  - supabase/config.toml
  - supabase/migrations/001_init_profiles.sql
  - supabase/migrations/002_quizzes.sql
  - supabase/migrations/003_questions_answers.sql
  - supabase/migrations/004_quiz_access.sql
  - supabase/migrations/005_sessions.sql
  - supabase/migrations/006_subscriptions.sql
  - supabase/migrations/007_rls_policies.sql
  - src/1-app/main.ts
  - src/1-app/App.vue
  - src/1-app/router/index.ts
  - src/1-app/styles/main.css
  - src/2-pages/AuthPage.vue
  - src/2-pages/QuizListPage.vue
  - src/2-pages/MyQuizListPage.vue
  - src/2-pages/QuizEditorPage.vue
  - src/4-features/auth/model/useAuthStore.ts
  - src/4-features/auth/model/useAuthStore.test.ts
  - src/4-features/auth/ui/LoginForm.vue
  - src/4-features/auth/ui/RegisterForm.vue
  - src/5-entities/quiz/api.ts
  - src/5-entities/quiz/model.ts
  - src/6-shared/api/supabase.ts
  - src/6-shared/api/database.types.ts
  - src/6-shared/config/env.ts
  - src/6-shared/types/index.ts
  - src/6-shared/lib/debounce.ts
  - src/6-shared/lib/image.ts
  - src/6-shared/lib/format.ts
  - src/6-shared/lib/draggable.ts
  - src/6-shared/ui/Button.vue
  - src/6-shared/ui/Input.vue
  - src/6-shared/ui/Dialog.vue
  - src/6-shared/ui/Tabs.vue
  - src/6-shared/ui/Switch.vue
  - src/6-shared/ui/Tooltip.vue
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "A new user can register with email + password and is redirected to the home page"
    - "A returning user can log in and remains authenticated after a browser refresh"
    - "An authenticated user can log out from the home page header"
    - "The home page performs a real anon SELECT against the quizzes table"
    - "All 7 database tables exist with RLS enabled in the Supabase project"
  artifacts:
    - path: "supabase/migrations/007_rls_policies.sql"
      provides: "RLS dual-policy set for all tables"
      contains: "anon_read_published_quizzes"
    - path: "src/4-features/auth/model/useAuthStore.ts"
      provides: "Auth session store with init/login/register/logout"
      exports: ["useAuthStore"]
    - path: "src/6-shared/api/supabase.ts"
      provides: "Supabase client singleton"
      exports: ["supabase"]
    - path: "src/1-app/router/index.ts"
      provides: "Router with beforeEach auth guard"
      exports: ["router"]
  key_links:
    - from: "src/4-features/auth/ui/LoginForm.vue"
      to: "useAuthStore.login"
      via: "form submit handler"
      pattern: "authStore\\.login"
    - from: "src/4-features/auth/model/useAuthStore.ts"
      to: "supabase.auth"
      via: "signInWithPassword / signUp / onAuthStateChange"
      pattern: "supabase\\.auth\\."
    - from: "src/2-pages/QuizListPage.vue"
      to: "fetchPublishedQuizzes"
      via: "page onMounted fetch"
      pattern: "fetchPublishedQuizzes"
---

<objective>
Establish the Walking Skeleton for Quiz Flow: scaffold the Vite + Vue 3 + TypeScript + Tailwind v4 project with the full FSD layer structure, apply all 7 Supabase migrations with RLS, and ship a real end-to-end auth slice — a user registers, the DB trigger creates their profile, the session persists across refresh, and they land on a home page that performs a real anon SELECT against `quizzes`.

Purpose: Proves the entire stack works together (scaffold → routing → auth → DB read/write → UI) and lays the FSD/Supabase foundation every later plan and phase builds on. Steiger is wired from day one to prevent layer drift.
Output: Working dev app at `npm run dev`, 7 migrations applied to Supabase, auth flow functional, 4 routes registered, shared `6-shared` foundation, 6 shadcn-vue components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@SPEC.md
@CLAUDE.md
@.planning/phases/01-foundation-auth-and-quiz-editor/SKELETON.md
@.planning/phases/01-foundation-auth-and-quiz-editor/01-RESEARCH.md
@.planning/phases/01-foundation-auth-and-quiz-editor/01-PATTERNS.md
@.planning/phases/01-foundation-auth-and-quiz-editor/01-CONTEXT.md
@.planning/phases/01-foundation-auth-and-quiz-editor/01-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Contracts established by this plan that Plans 02-04 consume. -->

QuizSettings JSONB shape (src/6-shared/types/index.ts):
  interface QuizSettings { allow_back: boolean; show_stop_button: boolean; shuffle_questions: boolean; shuffle_answers: boolean }

Quiz entity API (src/5-entities/quiz/api.ts) — verbatim from 01-PATTERNS.md:
  fetchMyQuizzes(): Promise<Quiz[]>
  fetchPublishedQuizzes(): Promise<Quiz[]>
  fetchQuiz(id: string): Promise<Quiz>
  createQuiz(): Promise<Quiz>
  updateQuiz(id: string, patch: Partial<Quiz>): Promise<void>
  deleteQuiz(id: string): Promise<void>

Auth store (src/4-features/auth/model/useAuthStore.ts):
  useAuthStore() → { user, isLoading, init(), login(email,password), register(email,password), logout() }

Path aliases (tsconfig.json + vite.config.ts): @app @pages @widgets @features @entities @shared
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold Vite + Vue + Tailwind v4 + FSD structure + steiger</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.node.json, index.html, eslint.config.ts, steiger.config.ts, .gitignore, .env.example, src/1-app/styles/main.css, src/1-app/main.ts, src/1-app/App.vue</files>
  <read_first>
    - .planning/phases/01-foundation-auth-and-quiz-editor/01-PATTERNS.md (verbatim templates for vite.config.ts, tsconfig.json, main.css, main.ts, App.vue, Pattern G shadcn-vue install)
    - .planning/phases/01-foundation-auth-and-quiz-editor/01-RESEARCH.md (Standard Stack table with exact versions; Pattern 8 Tailwind v4; Open Question 1 steiger)
    - .planning/research/PITFALLS.md (§4 FSD drift, §7 Tailwind v4)
  </read_first>
  <behavior>
    - `npm run dev` starts Vite without errors and serves a page
    - `npx steiger ./src` runs and reports zero violations on the empty FSD skeleton
    - `npm run build` (vue-tsc + vite build) completes with no type errors
    - `npm run lint` (eslint) completes with no errors
  </behavior>
  <action>
    Run `npm create vite@latest` equivalent setup OR scaffold manually: create package.json with production deps `vue pinia vue-router @supabase/supabase-js vue-draggable-plus vue-sonner lucide-vue-next vee-validate @vee-validate/zod zod` and dev deps `vite @vitejs/plugin-vue typescript vue-tsc @tailwindcss/vite tailwindcss steiger eslint @vue/eslint-config-typescript vitest @vue/test-utils @pinia/testing happy-dom` at the versions in 01-RESEARCH.md Standard Stack table.
    Create `vite.config.ts` with `vue()` and `tailwindcss()` plugins and the six `@app`…`@shared` resolve aliases mapping to `src/1-app`…`src/6-shared` (verbatim from 01-PATTERNS.md). Do NOT create `tailwind.config.js` and do NOT install `@tailwindcss/postcss`.
    Create `tsconfig.json` with `strict: true`, `moduleResolution: "bundler"`, and the six `@*/*` path entries; create `tsconfig.node.json` for the Vite config.
    Create `src/1-app/styles/main.css` with `@import "tailwindcss";`, `@theme { --font-sans: 'Inter', sans-serif; --color-brand-from: theme(--color-violet-600); --color-brand-to: theme(--color-indigo-600); }`, and `@custom-variant dark (&:where(.dark, .dark *));`. Do NOT use `@tailwind base/components/utilities` directives.
    Create `index.html` with `<div id="app">` and the Inter Google Fonts `<link>` tags.
    Create the empty FSD directory skeleton: `src/1-app`, `src/2-pages`, `src/3-widgets`, `src/4-features`, `src/5-entities`, `src/6-shared` (with subfolders `api lib ui config types`).
    Create `steiger.config.ts` configured for the numeric FSD prefix layers `1-app` through `6-shared` (per 01-RESEARCH.md Open Question 1 — verify steiger resolves numeric prefixes; add explicit layer config if it does not auto-detect).
    Create `eslint.config.ts` flat config using `@vue/eslint-config-typescript`.
    Create `src/1-app/main.ts` (createApp + createPinia + router + import main.css) and `src/1-app/App.vue` (RouterView + vue-sonner Toaster `position="top-right" richColors`, calls `useAuthStore().init()` in `onMounted`) verbatim from 01-PATTERNS.md.
    Add `dev build lint typecheck test` scripts to package.json. Create `.env.example` with `VITE_SUPABASE_URL=` and `VITE_SUPABASE_ANON_KEY=`. Add `.env`, `node_modules`, `dist` to `.gitignore`.
  </action>
  <verify>
    <automated>npm install && npm run build && npx steiger ./src</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with no TypeScript errors
    - `npx steiger ./src` exits 0 with zero reported violations
    - No file named `tailwind.config.js` exists in the project root
    - `src/1-app/styles/main.css` contains `@import "tailwindcss"` and does NOT contain `@tailwind base`
    - `vite.config.ts` contains `tailwindcss()` in the plugins array and all six `@`-prefixed aliases
    - Directories `src/1-app` through `src/6-shared` exist
  </acceptance_criteria>
  <done>Vite + Vue 3 + Tailwind v4 + FSD skeleton builds cleanly; steiger passes on the empty structure; dev server starts.</done>
</task>

<task type="auto">
  <name>Task 2: Author Supabase migrations 001-007 with RLS and create the covers bucket</name>
  <files>supabase/config.toml, supabase/migrations/001_init_profiles.sql, supabase/migrations/002_quizzes.sql, supabase/migrations/003_questions_answers.sql, supabase/migrations/004_quiz_access.sql, supabase/migrations/005_sessions.sql, supabase/migrations/006_subscriptions.sql, supabase/migrations/007_rls_policies.sql</files>
  <read_first>
    - .planning/phases/01-foundation-auth-and-quiz-editor/01-PATTERNS.md (verbatim SQL for 001, 002, 003, 007; Pattern F ENABLE RLS rule)
    - SPEC.md (§Supabase — схема таблиц: all 7 table definitions; §Миграции Supabase file list)
    - .planning/research/PITFALLS.md (§1 RLS dual-policy, §6 order_index no UNIQUE, §7.3 ENABLE RLS, §7.4 storage path)
    - .planning/phases/01-foundation-auth-and-quiz-editor/01-RESEARCH.md (Pattern 5 RLS, Code Examples migration 001, Open Question 4 covers bucket public)
  </read_first>
  <action>
    Create `supabase/config.toml` (Supabase CLI project config) or run `supabase init` to generate it.
    Author all 7 migration files. 001/002/003/007 copy verbatim from 01-PATTERNS.md. For 004/005/006, derive `CREATE TABLE` statements directly from the SPEC.md schema tables for `quiz_access`, `quiz_sessions`, `session_answers`, `subscriptions` — these are Phase 2/5 tables created now for schema completeness.
    Every `CREATE TABLE` must be immediately followed by `ALTER TABLE <name> ENABLE ROW LEVEL SECURITY` (Pattern F — no exceptions).
    001: `plan_type` enum, `profiles` table, `owner_own_profile` policy, `handle_new_user` SECURITY DEFINER function + `on_auth_user_created` trigger.
    002: `quizzes` table with `settings` JSONB DEFAULT `'{"allow_back":true,"show_stop_button":true,"shuffle_questions":false,"shuffle_answers":false}'`, indexes on `owner_id` and partial index on `is_published`.
    003: `question_type` enum, `questions` table (NO UNIQUE constraint on `(quiz_id, order_index)`), `answer_options` table, and the `answer_options_public` view selecting `id, question_id, body, order_index` (excludes `is_correct`).
    004: `quiz_access` table per SPEC.md (id, quiz_id FK, token uuid unique, login, password_hash, label, expires_at).
    005: `quiz_sessions` and `session_answers` tables per SPEC.md.
    006: `subscriptions` table per SPEC.md (id, user_id FK, plan, status enum, yookassa_payment_id, current_period_end).
    007: RLS dual-policy set verbatim from 01-PATTERNS.md — `owner_manage_*` policies `TO authenticated` using `(SELECT auth.uid())` subquery form, `anon_read_*` policies `TO anon` limited to `is_published = true`. NO anon SELECT on `answer_options` or `quiz_access`. Include the storage `covers` bucket INSERT policy comment block (path prefix `(storage.foldername(name))[1] = (SELECT auth.uid()::text)`).
    Add a statement (in 007 or a setup note) to create the public `covers` Storage bucket — either via SQL `INSERT INTO storage.buckets (id, name, public) VALUES ('covers','covers',true)` or document the `supabase` CLI / dashboard step in the SUMMARY.
    Always use `(SELECT auth.uid())` not bare `auth.uid()` in every policy.
  </action>
  <verify>
    <automated>grep -L "ENABLE ROW LEVEL SECURITY" supabase/migrations/00[1-6]*.sql; grep -c "(SELECT auth.uid())" supabase/migrations/007_rls_policies.sql</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files `supabase/migrations/001_*.sql` through `007_*.sql` exist
    - Every migration file 001-006 that contains `CREATE TABLE` also contains `ENABLE ROW LEVEL SECURITY` (grep -L returns empty)
    - `007_rls_policies.sql` contains `anon_read_published_quizzes` and `owner_manage_quizzes`
    - `007_rls_policies.sql` uses `(SELECT auth.uid())` and contains zero occurrences of bare `auth.uid()` outside the subquery form
    - `003_questions_answers.sql` contains `CREATE VIEW answer_options_public` and does NOT contain `UNIQUE` on `order_index`
    - `002_quizzes.sql` `settings` default contains `show_stop_button`
  </acceptance_criteria>
  <done>All 7 migration files authored; every table has RLS enabled; dual-policy set complete; covers bucket creation specified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Apply migrations to Supabase via supabase db push</name>
  <what-built>7 SQL migration files in `supabase/migrations/` and a `supabase/config.toml`. The schema is authored but not yet applied to a Supabase project.</what-built>
  <read_first>
    - SPEC.md (§Миграции Supabase)
    - .planning/phases/01-foundation-auth-and-quiz-editor/01-RESEARCH.md (Environment Availability — Supabase CLI 2.98.2 confirmed available)
  </read_first>
  <how-to-verify>
    This task requires a linked Supabase project and credentials that Claude cannot provision autonomously.
    1. Ensure a Supabase project exists. Set `SUPABASE_ACCESS_TOKEN` env var (non-TTY workaround) OR run `supabase login` interactively.
    2. Link the project: `supabase link --project-ref <ref>`.
    3. Apply the schema: `supabase db push`. Confirm all 7 migrations apply with no SQL errors.
    4. Create the public `covers` Storage bucket (if not created by SQL in migration 007): Supabase Dashboard → Storage → New bucket → name `covers`, public = true.
    5. Generate types: `supabase gen types typescript --linked > src/6-shared/api/database.types.ts`.
    6. Copy the project URL and anon key into a local `.env` file: `VITE_SUPABASE_URL=...` and `VITE_SUPABASE_ANON_KEY=...`.
    7. Confirm in the Supabase Dashboard → Database → Tables that all 7 tables exist and each shows RLS enabled.
  </how-to-verify>
  <action>
    Apply the 7 migration files to a Supabase project. Claude attempts this autonomously first: if `SUPABASE_ACCESS_TOKEN` is set and a project is linked, run `supabase db push` non-interactively. If credentials are missing, pause for the human steps in `<how-to-verify>` — provision/link the Supabase project, run `supabase db push`, create the public `covers` Storage bucket, run `supabase gen types typescript --linked > src/6-shared/api/database.types.ts`, and populate a local `.env` with `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`. This is a [BLOCKING] task — verification cannot proceed until the schema is applied to a real Supabase project.
  </action>
  <verify>
    <automated>test -f src/6-shared/api/database.types.ts && grep -q "quizzes" src/6-shared/api/database.types.ts</automated>
  </verify>
  <done>All 7 migrations applied to Supabase; 7 tables with RLS enabled; covers bucket created; database.types.ts generated; .env populated.</done>
  <acceptance_criteria>
    - `supabase db push` completes with all 7 migrations applied, no SQL errors
    - All 7 tables visible in the Supabase Dashboard with RLS enabled
    - `covers` Storage bucket exists and is public
    - `src/6-shared/api/database.types.ts` exists and contains a `Database` type with `quizzes`, `questions`, `answer_options` tables
    - Local `.env` contains real `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` values
  </acceptance_criteria>
  <resume-signal>Type "applied" once `supabase db push` succeeded, the covers bucket exists, database.types.ts is generated, and .env is populated. Or describe any SQL errors encountered.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Shared foundation — supabase client, types, libs, shadcn-vue components</name>
  <files>src/6-shared/api/supabase.ts, src/6-shared/config/env.ts, src/6-shared/types/index.ts, src/6-shared/lib/debounce.ts, src/6-shared/lib/image.ts, src/6-shared/lib/format.ts, src/6-shared/lib/draggable.ts, src/6-shared/ui/Button.vue, src/6-shared/ui/Input.vue, src/6-shared/ui/Dialog.vue, src/6-shared/ui/Tabs.vue, src/6-shared/ui/Switch.vue, src/6-shared/ui/Tooltip.vue, src/5-entities/quiz/api.ts, src/5-entities/quiz/model.ts</files>
  <read_first>
    - .planning/phases/01-foundation-auth-and-quiz-editor/01-PATTERNS.md (verbatim: supabase.ts, env.ts, types/index.ts QuizSettings, debounce.ts, image.ts, quiz/api.ts; Pattern G shadcn-vue install)
    - .planning/phases/01-foundation-auth-and-quiz-editor/01-RESEARCH.md (Don't Hand-Roll table; §Standard Stack shadcn-vue note)
    - SPEC.md (§quizzes table — for the Quiz model fields)
  </read_first>
  <action>
    Create `src/6-shared/config/env.ts` exporting typed `SUPABASE_URL` and `SUPABASE_ANON_KEY` from `import.meta.env`, throwing if either is missing (verbatim from 01-PATTERNS.md).
    Create `src/6-shared/api/supabase.ts` exporting the `supabase` singleton via `createClient<Database>(...)` — the only `createClient` call in the codebase.
    Create `src/6-shared/types/index.ts` with the `QuizSettings` interface (`allow_back`, `show_stop_button`, `shuffle_questions`, `shuffle_answers` — all boolean).
    Create `src/6-shared/lib/debounce.ts` with the hand-rolled `useDebounceFn` (no `@vueuse/core` dependency).
    Create `src/6-shared/lib/image.ts` with `resizeImageToMaxWidth(file, maxWidth = 1280): Promise<Blob>` (canvas resize, verbatim from 01-PATTERNS.md).
    Create `src/6-shared/lib/format.ts` with date and duration formatters (e.g., `formatDuration(sec)` → "10 мин").
    Create `src/6-shared/lib/draggable.ts` re-exporting `VueDraggable` from `vue-draggable-plus`.
    Run `npx shadcn-vue@latest init` then `npx shadcn-vue@latest add button input dialog tabs switch tooltip` so the 6 components are source-copied. Ensure they land under `src/6-shared/ui/` (configure the shadcn-vue components alias to `@shared/ui` during init).
    Create `src/5-entities/quiz/model.ts` with `Quiz` interface (id, owner_id, title, description, cover_url, time_limit_sec, is_published, settings: QuizSettings, created_at, updated_at) importing `QuizSettings` from `@shared/types`.
    Create `src/5-entities/quiz/api.ts` with `fetchMyQuizzes`, `fetchPublishedQuizzes`, `fetchQuiz`, `createQuiz`, `updateQuiz`, `deleteQuiz` verbatim from 01-PATTERNS.md. This file imports ONLY from `@shared` — never from `@features`.
  </action>
  <verify>
    <automated>npm run build && npx steiger ./src</automated>
  </verify>
  <acceptance_criteria>
    - `src/6-shared/api/supabase.ts` exports `supabase` and is the only file containing `createClient`
    - `src/6-shared/ui/` contains `Button.vue`, `Input.vue`, `Dialog.vue`, `Tabs.vue`, `Switch.vue`, `Tooltip.vue`
    - `src/5-entities/quiz/api.ts` exports all six functions and contains zero imports from `@features`
    - `src/6-shared/types/index.ts` contains `interface QuizSettings` with `show_stop_button`
    - `npm run build` exits 0; `npx steiger ./src` exits 0
  </acceptance_criteria>
  <done>Shared foundation complete; quiz entity API ready; shadcn-vue components source-copied; steiger passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Auth slice — store, login/register forms, routing, end-to-end auth flow</name>
  <files>src/4-features/auth/model/useAuthStore.ts, src/4-features/auth/model/useAuthStore.test.ts, src/4-features/auth/ui/LoginForm.vue, src/4-features/auth/ui/RegisterForm.vue, src/2-pages/AuthPage.vue, src/2-pages/QuizListPage.vue, src/2-pages/MyQuizListPage.vue, src/2-pages/QuizEditorPage.vue, src/1-app/router/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation-auth-and-quiz-editor/01-PATTERNS.md (verbatim: useAuthStore.ts, LoginForm/RegisterForm defineField pattern, AuthPage.vue, router/index.ts, QuizEditorPage.vue; Pattern B toast, Pattern D Pinia)
    - .planning/phases/01-foundation-auth-and-quiz-editor/01-CONTEXT.md (D-01, D-02, D-03, D-04)
    - .planning/phases/01-foundation-auth-and-quiz-editor/01-UI-SPEC.md (§AuthPage component anatomy, §Copywriting Contract, form states)
    - .planning/phases/01-foundation-auth-and-quiz-editor/01-RESEARCH.md (Pattern 2 auth, Pattern 7 router guard, Pitfall 8 defineField)
  </read_first>
  <behavior>
    - useAuthStore.test.ts: `init()` sets `user` from `getSession()` and subscribes to `onAuthStateChange` (mock supabase client)
    - useAuthStore.test.ts: `login()` calls `supabase.auth.signInWithPassword` and throws on error
    - useAuthStore.test.ts: `register()` calls `supabase.auth.signUp` and throws on error
    - useAuthStore.test.ts: `logout()` calls `supabase.auth.signOut` and clears `user`
  </behavior>
  <action>
    Create `src/4-features/auth/model/useAuthStore.ts` verbatim from 01-PATTERNS.md — Composition API store with `user`, `isLoading`, `init()` (getSession + onAuthStateChange), `login()`, `register()`, `logout()`.
    Write `useAuthStore.test.ts` (vitest + @pinia/testing + happy-dom) covering the four behaviors above with a mocked `@shared/api/supabase` module.
    Create `LoginForm.vue` and `RegisterForm.vue` using vee-validate `useForm` + `defineField` + `toTypedSchema(zod schema)` — email `.email('Некорректный email')`, password `.min(6,'Минимум 6 символов')`. Submit handler calls `authStore.login`/`authStore.register`, then `router.push(route.query.returnUrl || '/')` (D-01, D-03). On error show `toast.error` with the message (no inline field errors for auth per UI-SPEC). Loading state disables the button and shows the `Loader2` spinner. Forms have only email + password + submit — no `full_name`, no "Remember me", no OAuth (D-04). Use copy strings from the UI-SPEC Copywriting Contract.
    Create `AuthPage.vue` as a thin assembler: shadcn-vue `Tabs` with "Войти" / "Зарегистрироваться" triggers rendering `LoginForm`/`RegisterForm` — single `/auth` route, no URL change on toggle (D-02). Keep under ~80 lines.
    Create placeholder pages `QuizListPage.vue`, `MyQuizListPage.vue`, `QuizEditorPage.vue` so the router resolves. `QuizListPage.vue` MUST perform a real `fetchPublishedQuizzes()` call in `onMounted` and render the count or list (this is the skeleton's real DB read — Plan 02 builds the full UI). `MyQuizListPage` and `QuizEditorPage` may be minimal stubs that Plans 02/03 replace.
    Create `src/1-app/router/index.ts` verbatim from 01-PATTERNS.md — 4 routes (`/`, `/auth`, `/my`, `/editor/:id`), `beforeEach` guard redirecting unauthenticated access to `/my` and `/editor` to `/auth?returnUrl=...` (D-03).
  </action>
  <verify>
    <automated>npm run test -- --run src/4-features/auth/model/useAuthStore.test.ts && npm run build && npx steiger ./src</automated>
  </verify>
  <acceptance_criteria>
    - `useAuthStore.test.ts` exits 0 with all four behavior tests passing
    - `useAuthStore.ts` exports `useAuthStore` and calls `supabase.auth.signInWithPassword`, `signUp`, `signOut`, `onAuthStateChange`
    - `LoginForm.vue` and `RegisterForm.vue` use `defineField` and contain no `<Field>` component
    - `router/index.ts` registers 4 routes and a `beforeEach` guard checking `PROTECTED_ROUTES`
    - `QuizListPage.vue` contains a `fetchPublishedQuizzes` call
    - `npm run build` exits 0; `npx steiger ./src` exits 0
  </acceptance_criteria>
  <done>Auth store tested; login/register forms wired; router with guard registered; home page does a real anon SELECT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verify the end-to-end Walking Skeleton</name>
  <what-built>The complete Walking Skeleton: scaffolded app, 7 applied migrations, auth store + forms, router with guard, and a home page performing a real anon SELECT on `quizzes`.</what-built>
  <read_first>
    - .planning/phases/01-foundation-auth-and-quiz-editor/SKELETON.md (Capability Proven End-to-End)
    - .planning/phases/01-foundation-auth-and-quiz-editor/01-CONTEXT.md (D-01, D-03)
  </read_first>
  <how-to-verify>
    1. Run `npm run dev` and open the printed localhost URL.
    2. The home page `/` loads without console errors (it performs a real `fetchPublishedQuizzes()` — expect an empty list, no quizzes yet).
    3. Visit `/auth`, switch to the "Зарегистрироваться" tab, register with a fresh email + password (≥6 chars). Confirm you are redirected to `/` (D-01).
    4. In the Supabase Dashboard → Table editor → `profiles`: confirm a new row was auto-created for the registered user (the `handle_new_user` trigger fired).
    5. Refresh the browser. Confirm you remain authenticated (the header shows your email + "Выйти", not "Войти").
    6. While logged out (or in a private window), visit `/my` directly. Confirm you are redirected to `/auth?returnUrl=/my` (D-03).
    7. Log in, then click "Выйти". Confirm the session ends and the header reverts to "Войти".
  </how-to-verify>
  <action>
    Run the dev app and confirm the end-to-end Walking Skeleton works. Claude starts the dev server (`npm run dev`) and reports the URL; the human walks the 7 steps in `<how-to-verify>` covering registration, profile-row auto-creation, session persistence across refresh, the route guard, and logout. This checkpoint gates Phase 1 wave 1 — do not proceed to Plan 02 until the skeleton is verified working against the real Supabase project.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The Walking Skeleton is verified end-to-end: register → profile row → redirect → refresh-persist → guard → logout all work.</done>
  <acceptance_criteria>
    - Registration creates a `profiles` row and redirects to `/`
    - Session persists across a browser refresh
    - Unauthenticated access to `/my` redirects to `/auth?returnUrl=/my`
    - Logout ends the session from the home page
    - No uncaught errors in the browser console during the flow
  </acceptance_criteria>
  <resume-signal>Type "verified" once all 7 steps pass, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client → Supabase PostgREST | Untrusted user input crosses here; RLS is the enforcement layer |
| Browser client → Supabase Auth | Credentials cross here; Supabase handles token issuance and refresh |
| anon role → published quiz data | Guests (Phase 2) and logged-out users read published quizzes only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | All 7 tables | mitigate | `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` immediately after every `CREATE TABLE` (Task 2 acceptance criteria grep gate) |
| T-01-02 | Information Disclosure | `answer_options.is_correct` | mitigate | No anon SELECT on `answer_options`; `answer_options_public` view excludes `is_correct` (Task 2) |
| T-01-03 | Information Disclosure | `quiz_access.password_hash` | mitigate | No anon policy on `quiz_access` at all — Edge Function only (Task 2, 007 migration) |
| T-01-04 | Elevation of Privilege | `/my`, `/editor/:id` routes | mitigate | Vue Router `beforeEach` guard redirects unauthenticated users to `/auth` (Task 5) |
| T-01-05 | Tampering | PostgREST queries | mitigate | All queries via parameterized `@supabase/supabase-js` client; no raw SQL from client |
| T-01-06 | Spoofing | Auth session | mitigate | Supabase JWT in Authorization header (not cookie); auto-refresh via `onAuthStateChange` |
| T-01-07 | Information Disclosure | RLS performance / per-row eval | accept | `(SELECT auth.uid())` subquery form used for `initPlan` optimization — performance, not a vuln; documented |
| T-01-SC | Tampering | npm package installs | mitigate | All packages [VERIFIED] by provenance in 01-RESEARCH.md Package Legitimacy Audit; no [ASSUMED]/[SUS]/[SLOP] packages — no blocking checkpoint required |
</threat_model>

<verification>
- `npm run build` exits 0 (TypeScript clean)
- `npx steiger ./src` exits 0 (no FSD violations)
- `npm run test -- --run` passes the auth store tests
- All 7 migrations applied to Supabase; 7 tables with RLS enabled
- End-to-end auth flow verified manually (Task 6)
</verification>

<success_criteria>
- A new user can register and is redirected to `/`; their `profiles` row is auto-created
- A returning user stays authenticated across a browser refresh and can log out from any page
- The home page performs a real anon SELECT against `quizzes`
- The full FSD skeleton builds cleanly and passes steiger
- All 7 database tables exist with RLS enabled
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-and-quiz-editor/01-01-SUMMARY.md` when done.
</output>
