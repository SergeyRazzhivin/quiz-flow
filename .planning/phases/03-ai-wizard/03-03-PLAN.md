---
phase: 03-ai-wizard
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/2-pages/MyQuizListPage.vue
  - src/3-widgets/QuizEditorHeader.vue
  - src/4-features/quiz-list/ui/EmptyState.vue
  - evals/promptfooconfig.yaml
  - evals/quiz-schema.eval.test.ts
  - evals/dataset/.gitkeep
  - evals/judge-prompts/.gitkeep
autonomous: false
requirements: [AI-01]

must_haves:
  truths:
    - "An owner can launch the AI wizard from a 'Создать с ИИ' button on the /my page"
    - "An owner can launch the AI wizard from a 'Создать с ИИ' button in the quiz-editor header"
    - "Both entry buttons route to /ai-wizard and carry a tooltip"
    - "A Vitest eval suite asserts the D1-D3 code-decidable dimensions over recorded quiz generations"
  artifacts:
    - path: "evals/quiz-schema.eval.test.ts"
      provides: "D1-D3 schema/count/correct-count assertions using the real QuizSchema"
      contains: "QuizSchema"
    - path: "evals/promptfooconfig.yaml"
      provides: "Promptfoo prompt-regression config + D4-D6 LLM-judge rubric scaffold"
      contains: "providers"
  key_links:
    - from: "src/2-pages/MyQuizListPage.vue"
      to: "/ai-wizard"
      via: "router.push on the Создать с ИИ button"
      pattern: "ai-wizard"
    - from: "src/3-widgets/QuizEditorHeader.vue"
      to: "/ai-wizard"
      via: "router.push on the Создать с ИИ button"
      pattern: "ai-wizard"
---

<objective>
Wire the two D-02 entry points into the AI wizard and scaffold the AI-SPEC §5 evaluation
harness.

Purpose: The wizard route exists after plan 03-02 but is unreachable from the product UI;
this plan adds the two `Создать с ИИ` entry buttons. It also stands up the Vitest + Promptfoo
eval harness so quiz-generation quality has a regression gate from day one (AI-SPEC §5 — the
dataset is built concurrently, not after).
Output: entry buttons on `/my` and the editor header, and the `evals/` harness scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-wizard/03-CONTEXT.md
@.planning/phases/03-ai-wizard/03-UI-SPEC.md
@.planning/phases/03-ai-wizard/03-AI-SPEC.md
@.planning/phases/03-ai-wizard/03-PATTERNS.md

<interfaces>
<!-- Entry-button analogs — copy the surrounding markup exactly. -->
From src/2-pages/MyQuizListPage.vue:
- Existing button: `<Button v-if="quizzes.length > 0" @click="handleCreate"><Plus .../>Создать тест</Button>`.
- Add a sibling `Создать с ИИ` button next to it (and decide whether it also appears in the EmptyState).

From src/3-widgets/QuizEditorHeader.vue:
- Right-side group `<div class="ml-auto flex items-center gap-3">` holds the orange
  `Ссылки доступа` Button and `<PublishToggle />`.
- Add an `outline`-variant AI button to this group (keep it outline so two orange buttons don't compete).

<!-- 6-shared/ui — consume verbatim. -->
- `Button`, `Tooltip`; icon `Sparkles` from `lucide-vue-next`.

<!-- The real schema the eval imports. -->
From supabase/functions/_shared/quiz-schema.ts (plan 03-01):
- `QuizSchema` (Zod) and `GeneratedQuiz` type — the eval re-uses these exact exports.

<!-- AI-SPEC §5 — eval harness layout, dimensions, thresholds. -->
- D1 schema/shape, D2 correct-answer-count, D3 question-count — code-decidable (Vitest).
- D4 answer-key, D5 faithfulness, D6 item-quality — LLM judge (Promptfoo).
- Release threshold: D1-D3 = 100%; D4 >= 95%; D5 >= 90%; D6 mean >= 3.5/5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two D-02 entry-point buttons into /my and the editor header</name>
  <files>src/2-pages/MyQuizListPage.vue, src/3-widgets/QuizEditorHeader.vue, src/4-features/quiz-list/ui/EmptyState.vue</files>
  <read_first>
    - src/2-pages/MyQuizListPage.vue (the existing "Создать тест" Button and its handler)
    - src/3-widgets/QuizEditorHeader.vue (the right-side flex group with "Ссылки доступа" + PublishToggle)
    - src/4-features/quiz-list/ui/EmptyState.vue (the zero-quiz empty-state — decide if the AI entry appears here)
    - .planning/phases/03-ai-wizard/03-UI-SPEC.md (Entry-point buttons table — copy, icon, variant, tooltip)
  </read_first>
  <action>
    In `src/2-pages/MyQuizListPage.vue`, add a `Создать с ИИ` `<Button variant="default">`
    with a `Sparkles` icon (from `lucide-vue-next`) next to the existing `Создать тест`
    button, calling `router.push('/ai-wizard')`. In `src/3-widgets/QuizEditorHeader.vue`, add a
    `Создать с ИИ` `<Button variant="outline" size="sm">` with a `Sparkles` icon into the
    `ml-auto flex items-center gap-3` group, calling `router.push('/ai-wizard')` — do NOT pass
    the current quiz id (D-02: the wizard always creates a new quiz). Wrap both buttons in a
    `Tooltip` with the text `Сгенерировать новый тест из текста или файла`. In
    `src/4-features/quiz-list/ui/EmptyState.vue`, also surface the `Создать с ИИ` action
    alongside the existing create action so a zero-quiz owner can reach the wizard. Use the
    exact UI-SPEC copy verbatim.
  </action>
  <verify>
    <automated>grep -q "ai-wizard" src/2-pages/MyQuizListPage.vue && grep -q "ai-wizard" src/3-widgets/QuizEditorHeader.vue && grep -q "Создать с ИИ" src/2-pages/MyQuizListPage.vue && grep -q "Создать с ИИ" src/3-widgets/QuizEditorHeader.vue && npx vue-tsc --noEmit && npx steiger src && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/2-pages/MyQuizListPage.vue` has a `Создать с ИИ` button that routes to `/ai-wizard`.
    - `src/3-widgets/QuizEditorHeader.vue` has an `outline`-variant `Создать с ИИ` button that routes to `/ai-wizard`.
    - Both buttons use the `Sparkles` icon and carry the tooltip `Сгенерировать новый тест из текста или файла`.
    - The editor-header button passes no quiz id (D-02).
    - `npx vue-tsc --noEmit`, `npx steiger src`, and `npm run build` all pass.
  </acceptance_criteria>
  <done>Both entry points route to /ai-wizard with the correct copy, icon, variant, and tooltip; the build and FSD linter pass.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the evals harness — Vitest D1-D3 suite and Promptfoo config</name>
  <files>evals/quiz-schema.eval.test.ts, evals/promptfooconfig.yaml, evals/dataset/.gitkeep, evals/judge-prompts/.gitkeep, package.json</files>
  <read_first>
    - .planning/phases/03-ai-wizard/03-AI-SPEC.md (§5 — dimensions D1-D6, eval tooling, reference-dataset spec, CI thresholds, harness layout)
    - supabase/functions/_shared/quiz-schema.ts (plan 03-01 — the QuizSchema the eval imports)
    - package.json (existing Vitest setup and the `npm test` script)
  </read_first>
  <action>
    Add `promptfoo` as a dev dependency (`npm install --save-dev promptfoo`). Create the
    `evals/` directory. Create `evals/quiz-schema.eval.test.ts` — a Vitest suite that imports
    the real `QuizSchema` from `supabase/functions/_shared/quiz-schema.ts` and asserts the three
    code-decidable AI-SPEC §5 dimensions over recorded generation fixtures: D1 — every recorded
    output satisfies `QuizSchema.parse` without throwing and `finish_reason === 'stop'`; D2 —
    every `single` question has exactly one `is_correct=true` and every `multiple` has at least
    one (the `.refine()` holds); D3 — `quiz.questions.length` equals the requested count. The
    suite reads fixtures from `evals/dataset/`; with no fixtures yet it must be a passing suite
    with at least one placeholder/`it.todo` case so `npm test` stays green. Create
    `evals/promptfooconfig.yaml` — a Promptfoo config referencing the `_shared/quiz-prompt.ts`
    prompt under test, an OpenAI provider reading `OPENAI_API_KEY` from env with a fixed `seed`,
    `tests` pointing at `evals/dataset/`, and `llm-rubric` assertion scaffolds for D4
    (answer-key correctness >= 95%), D5 (source faithfulness >= 90%), and D6 (item quality mean
    >= 3.5/5). Create `evals/dataset/.gitkeep` and `evals/judge-prompts/.gitkeep` as
    placeholders for the 15-case reference dataset and the Russian-language judge prompts
    (populated incrementally per AI-SPEC §5 — built concurrently with later work, not now).
    Add an npm script `eval` running `npx promptfoo eval -c evals/promptfooconfig.yaml` and
    ensure `npm test` picks up `evals/*.eval.test.ts`.
  </action>
  <verify>
    <automated>test -f evals/quiz-schema.eval.test.ts && test -f evals/promptfooconfig.yaml && test -d evals/dataset && test -d evals/judge-prompts && grep -q QuizSchema evals/quiz-schema.eval.test.ts && grep -q promptfoo package.json && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `evals/quiz-schema.eval.test.ts` imports `QuizSchema` from `supabase/functions/_shared/quiz-schema.ts`.
    - The Vitest suite covers D1 (schema), D2 (correct-answer-count), D3 (question-count) and passes green (placeholder/`it.todo` allowed when no fixtures exist).
    - `evals/promptfooconfig.yaml` defines a provider, the prompt under test, and `llm-rubric` scaffolds for D4/D5/D6.
    - `evals/dataset/` and `evals/judge-prompts/` exist.
    - `promptfoo` is listed under `devDependencies` and an `eval` npm script exists.
    - `npm test` passes.
  </acceptance_criteria>
  <done>The evals harness exists — a green Vitest D1-D3 suite tied to the real QuizSchema and a Promptfoo config scaffolding the D4-D6 LLM-judge gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the entry points and the eval suite</name>
  <files>src/2-pages/MyQuizListPage.vue</files>
  <action>Confirm both Создать с ИИ entry buttons open the wizard and the evals Vitest suite is collected and green.</action>
  <what-built>The two `Создать с ИИ` entry buttons are wired into `/my`, the `/my` empty state, and the quiz-editor header. The evals harness is scaffolded.</what-built>
  <how-to-verify>
    1. Log in and open `/my`: confirm a `Создать с ИИ` button appears next to `Создать тест`, with the tooltip on hover; click it and confirm it opens `/ai-wizard`.
    2. With zero quizzes, confirm the empty state also offers the `Создать с ИИ` action.
    3. Open any quiz in the editor: confirm a `Создать с ИИ` outline button appears in the header next to `Ссылки доступа`; click it and confirm it opens `/ai-wizard` (a fresh wizard, not the current quiz).
    4. Run `npm test` and confirm the `evals/quiz-schema.eval.test.ts` suite is collected and green.
  </how-to-verify>
  <verify>
    <automated>MISSING — entry-point UX verification is a blocking-human checkpoint (visual + interaction confirmation)</automated>
  </verify>
  <acceptance_criteria>
    - Both `Создать с ИИ` buttons (on `/my` and in the editor header) open `/ai-wizard`.
    - The `/my` empty state also offers the AI entry.
    - `npm test` collects and passes `evals/quiz-schema.eval.test.ts`.
  </acceptance_criteria>
  <done>Both entry points work, the empty state offers the AI entry, and the eval suite is green; the user has approved.</done>
  <resume-signal>Type "approved" if both entry points work and the eval suite is green, or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /my and editor header to /ai-wizard | In-app navigation between authenticated owner views — no new external boundary |
| evals harness to OpenAI API | The Promptfoo gate calls OpenAI; runs in dev/CI only, never in the shipped app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Information Disclosure | evals OPENAI_API_KEY | mitigate | The eval run reads `OPENAI_API_KEY` from env / a gitignored `.env`; the key is never committed and the `evals/` harness is dev-only, not bundled into the app |
| T-03-15 | Spoofing | entry buttons | accept | The buttons only `router.push('/ai-wizard')`; the `requiresAuth` guard (plan 03-02) and the EF owner-auth (plan 03-01) are the real access controls — no new exposure |
| T-03-SC | Tampering | promptfoo dev install | mitigate | `promptfoo` is a well-known dev dependency; install verified against npmjs.com/package/promptfoo before commit |
</threat_model>

<verification>
- Both `Создать с ИИ` entry points navigate to `/ai-wizard`.
- The `/my` empty state also offers the AI entry.
- `npm test` collects and passes `evals/quiz-schema.eval.test.ts`.
- `npx vue-tsc --noEmit`, `npx steiger src`, and `npm run build` all pass.
</verification>

<success_criteria>
- The AI wizard is reachable from both D-02 entry points (completes AI-01).
- The AI-SPEC §5 eval harness is scaffolded with a green D1-D3 Vitest gate and a Promptfoo D4-D6 config.
</success_criteria>

<output>
Create `.planning/phases/03-ai-wizard/03-03-SUMMARY.md` when done.
</output>
