# Phase 3: AI Wizard - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-17
**Phase:** 3-ai-wizard
**Areas discussed:** Точка входа и навигация, Шаг 2 — источник материала, Шаг 3 — параметры генерации, Шаг 4 — прогресс и сбой

---

## Точка входа и навигация

### Q1 — Как AI-визард появляется на экране?

| Option | Description | Selected |
|--------|-------------|----------|
| Отдельный маршрут (полный экран) | Маршрут вроде /ai-wizard — отдельная страница | ✓ |
| Модалка поверх /my | Оверлей-окно над списком тестов | |
| Модалка из редактора | Кнопка AI в шапке редактора открывает модалку | |

**User's choice:** Отдельный маршрут (полный экран)

### Q2 — Откуда владелец запускает визард?

| Option | Description | Selected |
|--------|-------------|----------|
| Только с /my | Кнопка на «Мои тесты», всегда создаёт новый тест | |
| С /my и из шапки редактора | Кнопка и на /my, и в шапке редактора (как в SPEC) | ✓ |

**User's choice:** С /my и из шапки редактора

### Q3 — Когда создаётся сама запись теста в БД?

| Option | Description | Selected |
|--------|-------------|----------|
| Только после успешной генерации | Шаги 1–3 в памяти клиента; тест создаётся когда AI вернул JSON | ✓ |
| Сразу при старте визарда | Пустой тест создаётся сразу; брошенные визарды оставляют пустые тесты | |

**User's choice:** Только после успешной генерации

---

## Шаг 2 — источник материала

### Q1 — Как владелец задаёт исходный материал?

| Option | Description | Selected |
|--------|-------------|----------|
| Текст или файл (переключатель) | Две вкладки: вставить текст ИЛИ загрузить файл | |
| Текст и файл вместе | Файл + текстовый промпт одновременно | |
| Только текст (файлы — позже) | v1 без парсинга файлов | |

**User's choice:** Other — «Один источник за раз, но должно быть поле где вводиться дополнительный промпт описывающий, что данный тест проверяет (например если это книга, то на знание персонажей или событий)».
**Notes:** Уточнено: файлы остаются в scope v1 с ограничением по размеру. Дополнительный промпт — это уточняющий промпт к основному источнику (из файла или текста). Решено объединить его с «областью фокуса» AI-04 в одно поле на шаге 2.

### Q2 — Лимит файла и количество файлов

| Option | Description | Selected |
|--------|-------------|----------|
| Один файл, до 10 МБ | | |
| Один файл, до 5 МБ | | |
| Несколько файлов | | |

**User's choice:** Other — «1 файл до 1 мб для free версии; для платных пользователей размер файла может быть больше».

### Q3 — Лимит размера файла для Pro

| Option | Description | Selected |
|--------|-------------|----------|
| до 10 МБ | Free 1 МБ / Pro 10 МБ | |
| до 5 МБ | Free 1 МБ / Pro 5 МБ | ✓ |
| до 20 МБ | Free 1 МБ / Pro 20 МБ | |

**User's choice:** до 5 МБ
**Notes:** Лимит зависит от тарифа; реальный enforcement — Фаза 5, значения зафиксированы здесь.

---

## Шаг 3 — параметры генерации

### Q1 — Как владелец задаёт количество вопросов?

| Option | Description | Selected |
|--------|-------------|----------|
| Пресет-чипы (5/10/15/20) | Готовые варианты кнопками | |
| Числовое поле / слайдер | Свободный ввод числа в диапазоне | |
| Чипы + «Своё число» | Пресеты плюс ручной ввод | |

**User's choice:** Other — «числовое поле, для free 10 вопросов максимум для pro 100».

### Q2 — Как задаётся сложность генерации?

| Option | Description | Selected |
|--------|-------------|----------|
| 3 уровня: лёгкий / средний / сложный | Один выбор из трёх | |
| 5 уровней (шкала) | Детальная шкала | |
| Смешанная + 3 уровня | Три уровня плюс «смешанная» | |

**User's choice:** Other — «3 уровня плюс возможность описать промптом как регулировать степень сложности вопросов».

### Q3 — Кто решает тип вопросов (single / multiple)?

| Option | Description | Selected |
|--------|-------------|----------|
| AI решает сам | AI выбирает тип по смыслу вопроса | ✓ |
| Владелец выбирает | Переключатель only-single / only-multiple / mixed | |

**User's choice:** Other — «AI решает сам, но позже овнер может изменить, любой вопрос как угодно».

### Q4 — Поле «область фокуса» (AI-04)

| Option | Description | Selected |
|--------|-------------|----------|
| Одно поле на шаге 2 | Уточняющий промпт с шага 2 и есть «область фокуса» | ✓ |
| Отдельное поле на шаге 3 | Два разных поля | |

**User's choice:** Одно поле на шаге 2

---

## Шаг 4 — прогресс и сбой

### Q1 — Что видит владелец на шаге 4 во время генерации?

| Option | Description | Selected |
|--------|-------------|----------|
| Этапы со сменой статусов | Спиннер + сменяющиеся русские сообщения | ✓ |
| Прогресс-бар с % | Полоса прогресса с фиктивным процентом | |
| Этапы + индикатор шагов | Смена статусов + визуальные отметки этапов | |

**User's choice:** Этапы со сменой статусов

### Q2 — Что происходит при сбое генерации?

| Option | Description | Selected |
|--------|-------------|----------|
| Ошибка + повтор в визарде | Сообщение об ошибке и кнопка «Повторить» | |
| Ошибка + автоповтор 1 раз | EF пробует ещё раз, затем ошибка с ручным повтором | ✓ |
| Просто ошибка | Сообщение без кнопки повтора | |

**User's choice:** Ошибка + автоповтор 1 раз

### Q3 — Может ли владелец уйти со шага 4 и вернуться?

| Option | Description | Selected |
|--------|-------------|----------|
| Блокировать уход до конца | Владелец ждёт завершения на шаге 4 | ✓ |
| Можно уйти, задача идёт фоном | Генерация продолжается на сервере | |

**User's choice:** Блокировать уход до конца

---

## Claude's Discretion

- Точные русские формулировки статус-сообщений, ошибок, лейблов полей.
- Выбор модели OpenAI и prompt engineering.
- Точный набор Edge Functions и схема таблицы `ai_jobs`.
- Интервал поллинга статуса `ai_jobs`.
- Навигация по шагам визарда назад (стандартное поведение мастера до генерации).
- Подход к извлечению текста из PDF/DOCX.
- Визуальная стилизация в рамках дизайн-системы.

## Deferred Ideas

- Загрузка нескольких файлов / мульти-источник — v2.
- Реальный enforcement freemium-лимитов (размер файла, кол-во вопросов, квота 1 AI/мес) — Фаза 5.
- Прогресс-бар с процентами — отклонён в пользу честных этапов.
- Фоновая генерация с возвратом позже — отклонена (уход блокируется).
- Кнопка AI, дополняющая текущий тест вопросами — отклонена (визард всегда создаёт новый тест).
