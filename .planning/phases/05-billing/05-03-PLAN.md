---
phase: 05-billing
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/4-features/payment/model/usePaymentStore.ts
  - src/4-features/payment/ui/PricingCards.vue
  - src/4-features/payment/ui/ProStatusBanner.vue
  - src/3-widgets/BillingWidget.vue
  - src/2-pages/BillingPage.vue
  - src/3-widgets/AppHeader.vue
  - src/1-app/router/index.ts
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03, PAY-05]
user_setup: []

must_haves:
  truths:
    - "An owner can open /billing and see Free vs Pro plans side by side with feature lists"
    - "An owner can pick monthly or yearly and click Подписаться to start the YooKassa flow"
    - "A Pro owner sees a status banner with 'Pro активен до DD.MM.YYYY' and a Продлить подписку button"
    - "When a DB/EF limit error occurs, the user sees a toast with a 'Перейти на Pro' upsell action"
    - "D-07: there is no 'cancel subscription' concept — PAY-05 is satisfied purely by expiration, no manual cancel button"
    - "D-15: the pricing page uses two side-by-side cards (Free and Pro) with feature lists and a CTA; the current plan is highlighted"
    - "D-16: the period choice is a monthly (490 ₽) + yearly (4 490 ₽) toggle showing two prices"
    - "D-17: a Pro user on /billing sees status + 'Pro активен до DD.MM.YYYY' + a 'Продлить' (Renew) button"
    - "D-18: entry points are a 'Тарифы' header link plus contextual upsell prompts (toast/modal with 'Перейти на Pro') when a limited action is blocked"
  artifacts:
    - path: "src/2-pages/BillingPage.vue"
      provides: "/billing page"
    - path: "src/4-features/payment/model/usePaymentStore.ts"
      provides: "usage state + createPayment action"
      exports: ["usePaymentStore"]
    - path: "src/4-features/payment/ui/PricingCards.vue"
      provides: "Free + Pro card layout with period toggle"
  key_links:
    - from: "usePaymentStore.fetchUsage"
      to: "get_usage RPC"
      via: "supabase.rpc('get_usage')"
      pattern: "get_usage"
    - from: "BillingWidget"
      to: "usePaymentStore.fetchUsage"
      via: "onMounted call"
      pattern: "onMounted"
    - from: "AppHeader"
      to: "/billing route"
      via: "RouterLink"
      pattern: "to=\"/billing\""
---

<objective>
Build the billing frontend slice: the `payment` FSD feature (Pinia store + PricingCards + ProStatusBanner), the `BillingWidget` shell, the thin `BillingPage`, the `/billing` route, the AppHeader "Тарифы" link, and the limit-block upsell toast handler.

Purpose: PAY-02 (pricing page) and the UI half of PAY-03 (subscribe). Also surfaces PAY-01/PAY-05 enforcement to users — limit errors from plan 05-01 triggers and the 05-02 AI gate are caught and routed to an upsell toast (D-18).
Output: Five new frontend files + three modified files. This is the vertical slice that makes the billing loop usable end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-billing/05-CONTEXT.md
@.planning/phases/05-billing/05-UI-SPEC.md
@.planning/phases/05-billing/05-PATTERNS.md
@.planning/phases/05-billing/05-02-SUMMARY.md

<interfaces>
DB RPC from plan 05-01: `supabase.rpc('get_usage')` returns
`{ plan: 'free'|'pro', quizzes_used: number, quizzes_limit: number|null, ai_used: number, ai_limit: number, period_end: string|null }`.

Edge Function from plan 05-02: `create-payment` accepts `{ period: 'monthly'|'yearly' }` (Bearer JWT) and returns `{ confirmation_url, payment_id }`.

Store analog: `src/4-features/quiz-stats/model/useQuizStatsStore.ts` — composition store, `(supabase as any).rpc(...)`, try/catch/finally with `toast.error`, `useAuthStore` import allowed.
Component analog: `src/4-features/quiz-stats/ui/SummaryCards.vue` (props-driven display).
Page/widget analog: `src/2-pages/QuizStatsPage.vue` (thin) + `src/3-widgets/QuizStatsWidget.vue` (shell with `onMounted` fetch, `min-h-[100dvh] bg-neutral-950`, AppHeader, loading/error/data branches).
AppHeader nav: existing `<nav>` block, "/my" link guarded by `v-if="authStore.user"`.
Router: routes array with `meta: { requiresAuth: true }` shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: usePaymentStore + limit-error upsell handler</name>
  <files>src/4-features/payment/model/usePaymentStore.ts</files>
  <read_first>
    - src/4-features/quiz-stats/model/useQuizStatsStore.ts (store shape, RPC call, error discipline, toast usage)
    - .planning/phases/05-billing/05-RESEARCH.md (Pattern 6 — createPayment + window.location.href)
    - .planning/phases/05-billing/05-UI-SPEC.md (Interaction States, Copywriting Contract)
  </read_first>
  <behavior>
    - fetchUsage(): on success sets `usage` to the get_usage payload; on rpc error sets `error` and shows `toast.error` (does NOT silently downgrade to free).
    - isProActive computed: true only when `usage.plan === 'pro'` AND `usage.period_end` is a future date.
    - createPayment('monthly'): POSTs to create-payment with the session access_token, then assigns `window.location.href = confirmation_url`.
    - handleLimitError(err): if `err.message` includes `QUIZ_LIMIT_EXCEEDED` / `QUESTION_LIMIT_EXCEEDED` / `AI_LIMIT_EXCEEDED`, shows a `toast.error` with a `{ label: 'Перейти на Pro', onClick }` action navigating to /billing; returns true. Otherwise returns false.
  </behavior>
  <action>
    Create `src/4-features/payment/model/usePaymentStore.ts`. FSD header comment: 4-features imports from 5-entities and 6-shared only (`useAuthStore` import is the allowed exception). Composition-API `defineStore('payment', () => {...})`.
    State: `usage = ref<UsageData | null>(null)`, `loading = ref(false)`, `error = ref<string | null>(null)`. Define a `UsageData` type matching the get_usage shape (plan, quizzes_used, quizzes_limit, ai_used, ai_limit, period_end).
    `isProActive` computed per the behavior block.
    `fetchUsage()`: try/catch/finally; `(supabase as any).rpc('get_usage')`; `if (rpcError) throw rpcError`; on catch set `error` + `toast.error('Не удалось загрузить данные тарифа.')`.
    `createPayment(period: 'monthly'|'yearly')`: get session via `supabase.auth.getSession()`; `fetch` `${import.meta.env.VITE_SUPABASE_URL}/functions/v1/create-payment` with `Authorization: Bearer <token>`, `apikey`, `Content-Type: application/json`, body `{ period }`; on non-ok throw; on success `window.location.href = confirmation_url`.
    `handleLimitError(err: unknown)`: match the three limit tokens; use the vue-sonner action API with copy `'Перейти на Pro'` and `onClick` routing to `/billing` (import `router` from `@app/router` or accept a router instance — match how existing stores navigate). Copy the per-limit messages from 05-UI-SPEC Copywriting Contract verbatim.
    Add `$reset`. Return `usage, loading, error, isProActive, fetchUsage, createPayment, handleLimitError, $reset`.
  </action>
  <verify>
    <automated>grep -Ec "get_usage|create-payment|handleLimitError|QUIZ_LIMIT_EXCEEDED" src/4-features/payment/model/usePaymentStore.ts</automated>
  </verify>
  <acceptance_criteria>
    - `usePaymentStore` exported, store id is `'payment'`.
    - `fetchUsage` uses `supabase.rpc('get_usage')` and throws on rpc error (no silent free fallback).
    - `createPayment` redirects via `window.location.href`.
    - `handleLimitError` matches all three literal limit tokens and emits a toast with a 'Перейти на Pro' action.
    - `npx vue-tsc --noEmit` reports no new errors in this file.
  </acceptance_criteria>
  <done>usePaymentStore exposes usage state, createPayment redirect, and the limit-error upsell handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PricingCards + ProStatusBanner components</name>
  <files>src/4-features/payment/ui/PricingCards.vue, src/4-features/payment/ui/ProStatusBanner.vue</files>
  <read_first>
    - src/4-features/quiz-stats/ui/SummaryCards.vue (props-driven display component conventions, Tailwind classes)
    - src/6-shared/ui/Button.vue (variant API)
    - .planning/phases/05-billing/05-UI-SPEC.md (Component Specs — full PricingCards + ProStatusBanner spec, Copywriting, Color, Typography)
  </read_first>
  <behavior>
    - PricingCards: renders two cards (Free, Pro); a period toggle switches Pro price between "490 ₽/мес" and "4 490 ₽/год"; the user's current plan card shows "Текущий план" instead of a CTA.
    - PricingCards emits/calls createPayment on the Pro "Подписаться" click; button shows "Переходим к оплате…" + disabled while loading.
    - For a Free user with loaded usage, each metered Free feature shows "X из N" usage text.
    - ProStatusBanner: shows "Pro активен" + "Действует до DD.MM.YYYY" (ru-RU locale) + a "Продлить подписку" outline button.
  </behavior>
  <action>
    Create `src/4-features/payment/ui/PricingCards.vue`. Two-card grid `grid-cols-2 gap-6` (stacked on mobile). Free card `bg-neutral-900 rounded-xl border border-neutral-800 p-6`; Pro card `border-violet-600/40` with a `h-0.5 bg-gradient-to-r from-violet-600 to-indigo-600` top accent bar and a `PRO` gradient badge. Period toggle: segmented control, active `bg-neutral-700 text-neutral-50`; yearly shows a `Скидка 24%` chip `bg-violet-600/20 text-violet-400`. Pro CTA: gradient button `bg-gradient-to-r from-violet-600 to-indigo-600 text-white w-full`, label "Подписаться", loading label "Переходим к оплате…". Free CTA: secondary disabled button "Текущий план" when the user is Free. Feature lists exactly as listed in 05-UI-SPEC (Free: 3 теста / 10 вопросов на тест / 10 AI-генераций в месяц / Публичный доступ / strikethrough Индивидуальные ссылки / strikethrough Статистика по вопросам; Pro list per spec). Usage meter text `text-xs text-neutral-400` under Free items when `usage` is loaded and plan is free ("2 из 3 тестов", "X из 10 AI-генераций в этом периоде"). Checkmarks from `lucide-vue-next` `Check`. Do NOT use orange — gradient/violet only.
    Wire the Pro CTA to `usePaymentStore().createPayment(selectedPeriod)`.
    Create `src/4-features/payment/ui/ProStatusBanner.vue`. `bg-violet-600/10 border border-violet-600/30 rounded-xl px-6 py-4`, flex row: left `Sparkles` icon (`text-violet-400`) + "Pro активен" + sub-text "Действует до {date}" where date = `new Date(periodEnd).toLocaleDateString('ru-RU')`; right a `Button variant="outline" size="sm"` "Продлить подписку" that calls `createPayment`.
    Both components: props-driven; accept `usage` (and `loading` for PricingCards) so they stay testable. No domain logic beyond display + the store action call.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | grep -E "PricingCards|ProStatusBanner" | head -5; echo "tsc-done"</automated>
  </verify>
  <acceptance_criteria>
    - PricingCards renders Free and Pro cards with the exact feature-list copy from 05-UI-SPEC.
    - Period toggle switches the Pro price between `490 ₽` and `4 490 ₽`.
    - Pro CTA copy is "Подписаться", loading copy "Переходим к оплате…".
    - ProStatusBanner formats the date with `toLocaleDateString('ru-RU')` and shows "Продлить подписку".
    - No orange accent classes used; CTA uses the violet→indigo gradient.
    - `npx vue-tsc --noEmit` reports no errors in either file.
  </acceptance_criteria>
  <done>PricingCards and ProStatusBanner render per the 05-UI-SPEC visual contract and wire to createPayment.</done>
</task>

<task type="auto">
  <name>Task 3: BillingWidget shell, BillingPage, route, AppHeader link</name>
  <files>src/3-widgets/BillingWidget.vue, src/2-pages/BillingPage.vue, src/1-app/router/index.ts, src/3-widgets/AppHeader.vue</files>
  <read_first>
    - src/3-widgets/QuizStatsWidget.vue (widget shell: onMounted fetch, 100dvh shell, AppHeader, loading/error/data branches)
    - src/2-pages/QuizStatsPage.vue (thin page delegating to widget)
    - src/3-widgets/AppHeader.vue (nav block, "/my" link guard)
    - src/1-app/router/index.ts (routes array, requiresAuth meta)
    - .planning/phases/05-billing/05-UI-SPEC.md (Page Layout, AppHeader Extension)
  </read_first>
  <action>
    Create `src/3-widgets/BillingWidget.vue`. Shell: `min-h-[100dvh] bg-neutral-950`, `<AppHeader />`, `<main class="mx-auto max-w-3xl px-6 py-16">`. `onMounted(() => void store.fetchUsage())` — Pitfall 6: refreshes usage after the YooKassa return_url round-trip. Section heading "Тарифы" (`text-2xl font-semibold text-neutral-50`), subheading "Выберите подходящий план" (`text-sm text-neutral-400`). Branches: loading skeleton, error message, data. When `store.isProActive` render `<ProStatusBanner :usage="store.usage" />` above the cards. Always render `<PricingCards :usage="store.usage" :loading="store.loading" />`. Import the feature components from `@features/payment/ui/...` (3-widgets may compose feature slices).
    Create `src/2-pages/BillingPage.vue` as a thin assembler: `<script setup>` importing `BillingWidget from '@widgets/BillingWidget.vue'`, template renders `<BillingWidget />` only (~5 lines, mirrors QuizStatsPage).
    Modify `src/1-app/router/index.ts`: add route `{ path: '/billing', component: () => import('@pages/BillingPage.vue'), meta: { requiresAuth: true } }` to the routes array.
    Modify `src/3-widgets/AppHeader.vue`: add a `<RouterLink to="/billing" class="text-sm text-neutral-300 hover:text-neutral-50">Тарифы</RouterLink>` in the `<nav>` block, guarded by `v-if="authStore.user"` (same guard as "/my"), placed between "Мои тесты" and the right-side auth block.
  </action>
  <verify>
    <automated>grep -c "to=\"/billing\"" src/3-widgets/AppHeader.vue && grep -c "/billing" src/1-app/router/index.ts && npx steiger src --reporter=pretty 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `/billing` route exists in router with `meta: { requiresAuth: true }`.
    - AppHeader has a `Тарифы` RouterLink to `/billing` guarded by `v-if="authStore.user"`.
    - BillingWidget calls `store.fetchUsage()` in `onMounted` and conditionally renders ProStatusBanner.
    - BillingPage is a thin assembler delegating to BillingWidget.
    - `npx steiger src` reports no FSD layer violations.
  </acceptance_criteria>
  <done>The /billing page is reachable from the header, loads usage on mount, and shows the correct plan view.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of the billing page and limit upsell</name>
  <what-built>The /billing page (PricingCards, ProStatusBanner, BillingWidget), the AppHeader "Тарифы" link, and the limit-error upsell toast.</what-built>
  <how-to-verify>
    1. Run `npm run dev`. Log in as an owner.
    2. Click "Тарифы" in the header → confirm /billing loads with two cards (Free + Pro) side by side, dark theme, violet Pro accent (no orange).
    3. Toggle "Помесячно / Ежегодно" → confirm the Pro price switches between "490 ₽" and "4 490 ₽" and the "Скидка 24%" chip shows on yearly.
    4. As a Free user, confirm the Free card shows usage meters ("X из 3 тестов" etc.) and a disabled "Текущий план" button.
    5. Click "Подписаться" → confirm the button shows "Переходим к оплате…" and the browser navigates to YooKassa.
    6. As a Pro user, confirm the ProStatusBanner shows "Pro активен" + "Действует до DD.MM.YYYY" + "Продлить подписку".
    7. As a Free user with 3 quizzes, attempt to create a 4th → confirm a toast appears with a "Перейти на Pro" action that navigates to /billing.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/interaction issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → create-payment EF | Frontend store sends the session JWT; the EF re-verifies it |
| frontend display ← get_usage RPC | Client-displayed limits are UX hints only; DB/EF are authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | Client edits usage state to fake Pro | accept | client display is UX-only; DB triggers + EF gate enforce limits regardless (PAY-04) |
| T-05-13 | Information Disclosure | get_usage exposing another user's data | mitigate | get_usage resolves `auth.uid()` server-side; no user_id param accepted from client |
| T-05-14 | Spoofing | Forged confirmation_url redirect | accept | confirmation_url comes from create-payment EF response only; not user-supplied |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | no new packages this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npx vue-tsc --noEmit` passes with no new errors.
- `npx steiger src` reports no FSD layer violations (payment feature imports only 5-entities/6-shared + auth).
- The billing page renders both plan cards and the period toggle works.
</verification>

<success_criteria>
- An owner can reach /billing from the header and see Free vs Pro plans.
- The period toggle switches monthly/yearly pricing.
- Clicking Подписаться starts the YooKassa redirect flow.
- A Pro owner sees the status banner; limit blocks surface an upsell toast.
</success_criteria>

<output>
Create `.planning/phases/05-billing/05-03-SUMMARY.md` when done
</output>
</content>
