---
phase: 04-statistics
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/4-features/quiz-stats/model/useQuizStatsStore.ts
  - src/4-features/quiz-stats/ui/SummaryCards.vue
  - src/4-features/quiz-stats/ui/ResultsTable.vue
  - src/4-features/quiz-stats/ui/AccuracySection.vue
  - src/3-widgets/QuizStatsWidget.vue
  - src/2-pages/QuizStatsPage.vue
  - src/1-app/router/index.ts
  - src/3-widgets/QuizEditorHeader.vue
  - src/5-entities/quiz/ui/QuizCard.vue
autonomous: true
requirements: [STATS-01, STATS-02, STATS-03]

must_haves:
  truths:
    - "D-01: A dedicated authenticated route /quiz/:id/stats exists, and a 'Статистика' entry button on both the /my quiz card and the editor header navigates to it"
    - "An owner can open /quiz/:id/stats and see total attempts, completion rate %, and average score"
    - "An owner sees a per-person table — one row per taker, latest finished attempt — with name, score, completion timestamp"
    - "D-05: isPro is derived from a real read of the subscriptions table (plan + status), with no hardcoded gate"
    - "D-06: A Free owner sees the accuracy section blurred with an upgrade CTA and the get_quiz_accuracy RPC is never called for Free owners"
    - "A Pro owner sees per-question accuracy bars"
    - "D-07: Summary is rendered as cards, per-question accuracy as horizontal progress bars, per-person results as a comparison table — no chart library"
    - "D-08: A quiz with no attempts shows a friendly empty state, not zeroed cards or an error"
  artifacts:
    - path: "src/4-features/quiz-stats/model/useQuizStatsStore.ts"
      provides: "Pinia store: loadStats, isPro gate, completionRate, stats/accuracy state"
      contains: "get_quiz_accuracy"
    - path: "src/3-widgets/QuizStatsWidget.vue"
      provides: "Widget composing loading/empty/data states + AppHeader shell"
    - path: "src/2-pages/QuizStatsPage.vue"
      provides: "Thin assembler page for /quiz/:id/stats"
    - path: "src/4-features/quiz-stats/ui/AccuracySection.vue"
      provides: "Pro-gated accuracy bars with blur overlay"
  key_links:
    - from: "useQuizStatsStore.loadStats"
      to: "supabase.rpc('get_quiz_accuracy')"
      via: "call guarded by isPro.value === true"
      pattern: "if \\(isPro"
    - from: "QuizStatsWidget"
      to: "useQuizStatsStore"
      via: "onMounted store.loadStats(route.params.id)"
      pattern: "loadStats"
    - from: "router/index.ts"
      to: "QuizStatsPage"
      via: "/quiz/:id/stats route with requiresAuth"
      pattern: "/quiz/:id/stats"
---

<objective>
Build the quiz-stats FSD feature slice: a Pinia store calling the migration 013 RPCs, three section components (summary cards, per-person table, Pro-gated accuracy), a composing widget, a thin page, the new route, and the two "Статистика" entry buttons.

Purpose: Deliver the owner-facing statistics surface — STATS-01/02/03 — with the D-06 Pro gate enforced by not fetching accuracy data for Free owners.
Output: new quiz-stats slice, QuizStatsWidget, QuizStatsPage, /quiz/:id/stats route, entry buttons on QuizCard and QuizEditorHeader.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-statistics/04-CONTEXT.md
@.planning/phases/04-statistics/04-RESEARCH.md
@.planning/phases/04-statistics/04-PATTERNS.md
@.planning/phases/04-statistics/04-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor builds against. From RESEARCH Pattern 2 and migration 013 (plan 04-01). -->

migration 013 RPCs (already live after plan 04-01):
  supabase.rpc('get_quiz_stats', { p_quiz_id: string })
    → { totalAttempts: number, finishedCount: number, avgScore: number | null,
        totalQuestions: number, perPerson: PerPersonRow[] }
  supabase.rpc('get_quiz_accuracy', { p_quiz_id: string })
    → AccuracyRow[]

Store types (define in useQuizStatsStore.ts):
  PerPersonRow { name: string | null; score: number | null; finished_at: string }
  AccuracyRow  { question_id: string; body: string; order_index: number; accuracy_percent: number | null }
  QuizStats    { totalAttempts: number; finishedCount: number; avgScore: number | null;
                 totalQuestions: number; perPerson: PerPersonRow[] }

Shared helpers (from plan 04-01, in @shared/lib/format):
  formatPercent(value: number | null): string
  formatScore(score: number | null, totalQuestions: number): string
  formatShortDateTime(isoString: string): string

ProgressBar (from plan 04-01): <ProgressBar :value="0-100" size="sm" | "md" />
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: quiz-stats Pinia store with Pro-gated accuracy fetch</name>
  <files>src/4-features/quiz-stats/model/useQuizStatsStore.ts</files>
  <read_first>
    - .planning/phases/04-statistics/04-RESEARCH.md (Pattern 2, lines 285-377 — full store skeleton to copy; Pitfall 1, Pitfall 5)
    - .planning/phases/04-statistics/04-PATTERNS.md (store section, lines 103-151)
    - src/4-features/quiz-share/model/useQuizShareStore.ts (composition-API store analog: refs, try/catch+toast+finally, return object)
    - src/4-features/auth/model/useAuthStore.ts (user.id source for the subscriptions query)
    - src/6-shared/api/supabase.ts (the supabase singleton)
  </read_first>
  <behavior>
    - loadStats(quizId) sets isLoading true, loads Pro status, calls get_quiz_stats, populates stats.
    - When isPro is false, get_quiz_accuracy is NEVER called and accuracy stays null (D-06).
    - When isPro is true, get_quiz_accuracy is called and accuracy is populated.
    - A null / absent subscriptions row is treated as Free (Pitfall 5 — maybeSingle, optional chaining).
    - completionRate computed returns 0 when totalAttempts is 0, else round(finishedCount/totalAttempts*100).
    - A failed RPC sets error and fires toast.error; isLoading ends false via finally.
  </behavior>
  <action>
    Create src/4-features/quiz-stats/model/useQuizStatsStore.ts, copying the store skeleton from RESEARCH Pattern 2 verbatim. defineStore('quiz-stats', ...) composition style. Export interfaces PerPersonRow, AccuracyRow, QuizStats (shapes in the interfaces block above). State refs: stats, accuracy, isPro, isLoading, error. Computed completionRate per the behavior block.

    loadProStatus() (D-05 — Pro status from the real subscriptions table, no hardcoded gate): if no authStore.user, return; query supabase.from('subscriptions').select('plan, status').eq('user_id', authStore.user.id).maybeSingle(); set isPro = data?.plan === 'pro' && data?.status === 'active' (optional chaining handles the null row — Pitfall 5).

    loadStats(quizId): try/catch/finally. Call loadProStatus() first. Call supabase.rpc('get_quiz_stats', { p_quiz_id: quizId }); throw on error; assign stats. Only if isPro.value is true, call supabase.rpc('get_quiz_accuracy', { p_quiz_id: quizId }) and assign accuracy — NEVER call it for Free owners (D-06, Pitfall 1). On any error set error to the Russian message and toast.error per the UI-SPEC error copy. Provide a $reset that clears all state. Return stats, accuracy, isPro, isLoading, error, completionRate, loadStats, $reset.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit -p tsconfig.app.json && grep -q "if (isPro" src/4-features/quiz-stats/model/useQuizStatsStore.ts && grep -q "maybeSingle" src/4-features/quiz-stats/model/useQuizStatsStore.ts</automated>
  </verify>
  <acceptance_criteria>
    - Store exports useQuizStatsStore plus the three interfaces.
    - D-05: isPro is derived from a maybeSingle read of the subscriptions table (plan === 'pro' && status === 'active') — no hardcoded value.
    - D-06: get_quiz_accuracy is called only inside an `if (isPro.value)` branch; a Free owner never triggers the call.
    - subscriptions read uses maybeSingle and optional chaining; a null row yields isPro = false.
    - completionRate returns 0 when totalAttempts is 0.
    - vue-tsc passes with no new errors.
  </acceptance_criteria>
  <done>Store created; accuracy fetch is Pro-gated; type check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Section components — SummaryCards, ResultsTable, AccuracySection</name>
  <files>src/4-features/quiz-stats/ui/SummaryCards.vue, src/4-features/quiz-stats/ui/ResultsTable.vue, src/4-features/quiz-stats/ui/AccuracySection.vue</files>
  <read_first>
    - .planning/phases/04-statistics/04-UI-SPEC.md (Component & Layout Inventory, Copywriting Contract, Color, Typography)
    - .planning/phases/04-statistics/04-PATTERNS.md (SummaryCards lines 154-187, ResultsTable lines 189-221, AccuracySection lines 223-278)
    - .planning/phases/04-statistics/04-RESEARCH.md (Pattern 3 blur overlay lines 379-417, Code Examples lines 556-589)
    - src/4-features/quiz-share/ui/AccessLinkList.vue (table/list component analog)
    - src/6-shared/ui/Button.vue (variant/size props)
    - src/6-shared/ui/ProgressBar.vue (extended size prop from plan 04-01)
  </read_first>
  <behavior>
    - SummaryCards renders 3 cards: Всего попыток (totalAttempts), Процент завершений (completionRate as NN%), Средний балл (formatScore(avgScore, totalQuestions)).
    - SummaryCards shows animate-pulse skeletons while isLoading.
    - ResultsTable renders one row per perPerson entry, default sorted finished_at DESC; clicking a column header toggles sort key/direction.
    - ResultsTable formats score via formatScore and timestamp via formatShortDateTime.
    - AccuracySection with isPro false renders 4 skeleton bars behind a backdrop-blur-md overlay with Lock icon, Pro copy, and the "Перейти на Pro" CTA — and renders NO real numbers.
    - AccuracySection with isPro true renders one ProgressBar size="md" row per AccuracyRow with formatPercent label.
  </behavior>
  <action>
    Create the three components in src/4-features/quiz-stats/ui/, each script-setup + TypeScript, using the exact Tailwind classes and Russian copy from UI-SPEC and the analog snippets in 04-PATTERNS.md. D-07 governs the rendering style: cards for the summary, horizontal progress bars for accuracy, a comparison table for per-person results — built with existing UI primitives and Tailwind, no chart library.

    SummaryCards.vue (D-07 — cards with large numbers) — props: stats: QuizStats | null, completionRate: number, isLoading: boolean. Grid grid-cols-1 sm:grid-cols-3 gap-6. Each card bg-neutral-900 border border-neutral-800 rounded-xl p-6; label is text-[13px] uppercase tracking-wide text-neutral-400; number is text-4xl font-semibold text-neutral-50. Card 2 value is completionRate + '%'; card 3 value is formatScore(stats.avgScore, stats.totalQuestions). When isLoading, render three animate-pulse rounded-xl bg-neutral-800 h-28 placeholders instead.

    ResultsTable.vue (D-07 — comparison table) — props: rows: PerPersonRow[], totalQuestions: number. bg-neutral-900 border border-neutral-800 rounded-xl overflow-hidden, wrapped in overflow-x-auto. Header row bg-neutral-800 with Имя · Балл · Завершён (grid-cols-3, label style). Body rows border-t border-neutral-800, hover:bg-neutral-800/50. Local sortKey ref ('name' | 'score' | 'finished_at') and sortDir ref ('asc' | 'desc'), default 'finished_at' / 'desc'; a computed sortedRows; header click toggles. Cells: name (or a dash fallback), formatScore(row.score, totalQuestions), formatShortDateTime(row.finished_at).

    AccuracySection.vue (D-07 horizontal progress bars + D-06 Pro gate) — props: accuracy: AccuracyRow[] | null, isPro: boolean. Outer relative rounded-xl border border-neutral-800 bg-neutral-900 p-6 with h2 "Точность по вопросам". When !isPro (D-06): render 4 animate-pulse h-6 skeleton bars and an absolute inset-0 backdrop-blur-md bg-neutral-950/60 overlay with the Lock icon (lucide-vue-next), heading "Точность по вопросам — функция Pro", the UI-SPEC body copy, and a Button variant="default" size="sm" "Перейти на Pro" that router.push('/billing') — render NO real numbers. When isPro: one row per accuracy entry — truncated question body, ProgressBar :value="row.accuracy_percent ?? 0" size="md", and formatPercent(row.accuracy_percent) right-aligned. Import formatPercent/formatScore/formatShortDateTime from @shared/lib/format.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit -p tsconfig.app.json && npx steiger src && grep -q "backdrop-blur-md" src/4-features/quiz-stats/ui/AccuracySection.vue && grep -q "sortKey" src/4-features/quiz-stats/ui/ResultsTable.vue</automated>
  </verify>
  <acceptance_criteria>
    - Three components exist under src/4-features/quiz-stats/ui/.
    - D-07: SummaryCards uses cards, AccuracySection uses horizontal ProgressBar rows, ResultsTable is a comparison table — no chart library imported.
    - SummaryCards shows skeletons when isLoading and three labelled cards otherwise.
    - ResultsTable sorts client-side, defaults finished_at DESC, toggles on header click.
    - D-06: AccuracySection renders no real numbers and shows the blur+CTA overlay when isPro is false.
    - vue-tsc and steiger both pass (FSD layer discipline intact).
  </acceptance_criteria>
  <done>Three section components built per UI-SPEC; type + FSD checks green.</done>
</task>

<task type="auto">
  <name>Task 3: Widget, page, route, and "Статистика" entry buttons</name>
  <files>src/3-widgets/QuizStatsWidget.vue, src/2-pages/QuizStatsPage.vue, src/1-app/router/index.ts, src/3-widgets/QuizEditorHeader.vue, src/5-entities/quiz/ui/QuizCard.vue</files>
  <read_first>
    - .planning/phases/04-statistics/04-PATTERNS.md (widget lines 53-101, page lines 31-49, router lines 403-416, QuizEditorHeader lines 420-447, QuizCard lines 451-487, Empty State + Page Shell shared patterns lines 530-554)
    - .planning/phases/04-statistics/04-UI-SPEC.md (Empty state, Loading state, Interaction Contract)
    - src/3-widgets/QuizTakingWidget.vue (widget analog: useRoute, onMounted store init, conditional rendering)
    - src/2-pages/AiWizardPage.vue (thin-assembler page analog)
    - src/3-widgets/QuizEditorHeader.vue (existing Button row + lucide import — file being modified)
    - src/5-entities/quiz/ui/QuizCard.vue (existing actions row + lucide import — file being modified)
    - src/1-app/router/index.ts (route table — file being modified)
  </read_first>
  <action>
    QuizStatsWidget.vue (3-widgets) — script-setup; useRoute(); useQuizStatsStore(); onMounted calls store.loadStats(route.params.id as string). Page shell: outer div min-h-[100dvh] bg-neutral-950, then AppHeader, then main mx-auto max-w-5xl px-4 py-12 with an h1 "Статистика" (text-xl font-semibold). Render branches: store.isLoading → skeleton layout (SummaryCards with isLoading plus skeleton blocks); store.error → the UI-SPEC error heading/body block; store.stats && store.stats.totalAttempts === 0 → empty state (D-08 — friendly, not zeroed cards: centered py-16, lucide BarChart3 48px text-neutral-600, heading "Пока никто не проходил тест", body from UI-SPEC); else → SummaryCards + ResultsTable + AccuracySection stacked with gap-8 (xl spacing). Pass props down: SummaryCards gets stats/completionRate/isLoading; ResultsTable gets stats.perPerson and stats.totalQuestions; AccuracySection gets store.accuracy and store.isPro.

    QuizStatsPage.vue (2-pages) — thin assembler: import QuizStatsWidget from @widgets/QuizStatsWidget.vue and render only <QuizStatsWidget />. No store import, no onMounted.

    router/index.ts (D-01 — dedicated authenticated route) — add a route after the /editor/:id entry: path '/quiz/:id/stats', component lazy import of @pages/QuizStatsPage.vue, meta { requiresAuth: true }. Param is :id (RESEARCH Pitfall 6 — not :quizId).

    QuizEditorHeader.vue (D-01 — editor-header entry button) — add BarChart3 to the lucide-vue-next import; add a Button variant="outline" size="sm" labelled "Статистика" with a BarChart3 icon in the ml-auto action row, before the existing "Создать с ИИ" button, guarded v-if="editorStore.quiz", @click router.push(`/quiz/${editorStore.quiz.id}/stats`).

    QuizCard.vue (D-01 — /my quiz-card entry button) — add BarChart3 to the lucide-vue-next import; add a Button variant="ghost" size="sm" class="flex-1" labelled "Статистика" with a BarChart3 icon in the actions row between "Изменить" and the Trash2 button, @click router.push(`/quiz/${quiz.id}/stats`). useRouter is already imported.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit -p tsconfig.app.json && npx steiger src && npm run build && grep -q "/quiz/:id/stats" src/1-app/router/index.ts && grep -l "Статистика" src/3-widgets/QuizEditorHeader.vue src/5-entities/quiz/ui/QuizCard.vue</automated>
  </verify>
  <acceptance_criteria>
    - D-01: /quiz/:id/stats route registered with requiresAuth and lazy-imports QuizStatsPage.
    - QuizStatsPage is a pure assembler rendering only QuizStatsWidget.
    - QuizStatsWidget mounts the store, has loading / empty / data / error branches, uses min-h-[100dvh] and max-w-5xl.
    - D-08: the empty state is a friendly message ("Пока никто не проходил тест"), not zeroed cards or an error.
    - D-01: "Статистика" buttons appear on QuizCard and QuizEditorHeader, both navigating to /quiz/:id/stats.
    - vue-tsc, steiger, and npm run build all pass.
  </acceptance_criteria>
  <done>Route, page, widget, and both entry buttons wired; build and FSD checks green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Free owner client → get_quiz_accuracy | The accuracy RPC exists and is callable by any authenticated owner; the Pro gate is the client deciding not to call it |
| route guard → /quiz/:id/stats | Unauthenticated request must be redirected to /auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Elevation of Privilege | useQuizStatsStore.loadStats | mitigate | get_quiz_accuracy is called only inside `if (isPro.value)`; a Free client never receives accuracy numbers (D-06, RESEARCH Pitfall 1). isPro derives from the real subscriptions table (D-05), not a hardcoded flag |
| T-04-05 | Information Disclosure | /quiz/:id/stats route | mitigate | Route carries meta.requiresAuth; the existing router.beforeEach guard redirects unauthenticated users to /auth. The migration 013 RPC additionally rejects non-owners server-side |
| T-04-06 | Tampering | Pro gate is client-side | accept | A Free owner could call get_quiz_accuracy directly via devtools — accepted: per D-06 the gate is "do not fetch"; the RPC still enforces quiz ownership so only the owner's own data is ever exposed, and real subscription enforcement is Phase 5 scope |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase (RESEARCH: zero new dependencies) |
</threat_model>

<verification>
- /quiz/:id/stats renders summary cards, per-person table, and the accuracy section for an owner
- Free owner: accuracy section is blurred with CTA, get_quiz_accuracy is not called (network tab confirms)
- Pro owner: accuracy bars render real percentages
- Quiz with no attempts shows the empty state
- "Статистика" buttons on /my quiz cards and the editor header navigate correctly
- vue-tsc, steiger, npm run build all pass
</verification>

<success_criteria>
- STATS-01: owner sees total attempts, completion rate %, average score
- STATS-02: owner sees the per-person latest-attempt result table
- STATS-03: Pro owner sees per-question accuracy; Free owner sees the gated section
- FSD layer discipline holds (steiger green); the page is a thin assembler
</success_criteria>

<output>
Create `.planning/phases/04-statistics/04-02-SUMMARY.md` when done
</output>
</content>
</invoke>
