---
phase: 06-landing-page-service-overview-public-quiz-carousel-and-recen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/5-entities/quiz/api.ts
  - src/3-widgets/AppHeader.vue
  - src/3-widgets/AppFooter.vue
autonomous: true
requirements: []
must_haves:
  truths:
    - "A dedicated carousel fetcher returns published quizzes newest-first by updated_at, capped at 12, with question_count populated (D-07, D-08)"
    - "The existing fetchPublishedQuizzes is unchanged so QuizListPage keeps its created_at ordering"
    - "The 'Все тесты' nav link in AppHeader points to /quizzes, not / (D-01)"
    - "The 'Все тесты' nav link in AppFooter points to /quizzes, not / (D-01)"
    - "The AppHeader logo link still points to / (the landing page)"
  artifacts:
    - path: "src/5-entities/quiz/api.ts"
      provides: "fetchCarouselQuizzes(limit) entity fetcher"
      contains: "fetchCarouselQuizzes"
    - path: "src/3-widgets/AppHeader.vue"
      provides: "Catalog nav link rebinding"
    - path: "src/3-widgets/AppFooter.vue"
      provides: "Catalog nav link rebinding"
  key_links: []
---

<objective>
Build the data and navigation foundation for the landing page: a dedicated carousel
fetcher for the public quiz showcase, and the nav-link rebinding in shared chrome so
"Все тесты" routes to the new catalog path /quizzes.

Purpose: Plan 02 (landing widgets + router rebinding) consumes fetchCarouselQuizzes
and depends on the catalog route existing. This plan delivers everything that does
NOT require the new LandingPage.vue file to exist, so Plan 02 stays focused on UI.
Output: fetchCarouselQuizzes in the quiz entity API; updated nav links in AppHeader/AppFooter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-landing-page-service-overview-public-quiz-carousel-and-recen/06-CONTEXT.md
@.planning/phases/06-landing-page-service-overview-public-quiz-carousel-and-recen/06-RESEARCH.md
@.planning/phases/06-landing-page-service-overview-public-quiz-carousel-and-recen/06-PATTERNS.md

<interfaces>
From src/5-entities/quiz/api.ts — the fetchMyQuizzes flatten pattern (the analog for the new function):
- `fetchMyQuizzes()` selects `*, question_count:questions(count)`, orders by created_at desc,
  and flattens the aggregate via `agg?.[0]?.count ?? 0`.
- `fetchPublishedQuizzes()` selects `id, title, description, cover_url, time_limit_sec`,
  filters `is_published=true`, orders by `created_at` ascending:false. MUST NOT be modified.
- `Quiz` type is imported from `./model`; `question_count` is optional on it.

From src/3-widgets/AppHeader.vue — the logo link is `<RouterLink to="/">` (stays as `/`);
a separate nav `<RouterLink to="/">Все тесты</RouterLink>` is the one that must become `/quizzes`.

From src/3-widgets/AppFooter.vue — one `<RouterLink to="/">` wrapping "Все тесты" must become `/quizzes`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fetchCarouselQuizzes entity fetcher</name>
  <read_first>
    - src/5-entities/quiz/api.ts (the file being modified — read fetchMyQuizzes and fetchPublishedQuizzes in full)
    - src/5-entities/quiz/model.ts (the Quiz type and question_count optionality)
    - .planning/phases/06-landing-page-service-overview-public-quiz-carousel-and-recen/06-PATTERNS.md (section "src/5-entities/quiz/api.ts" — the exact recommended function body)
  </read_first>
  <action>
    Append a new exported async function `fetchCarouselQuizzes(limit = 12): Promise<Quiz[]>` to
    src/5-entities/quiz/api.ts, after fetchPublishedQuizzes. It queries `quizzes` selecting
    `id, title, description, cover_url, time_limit_sec, updated_at, question_count:questions(count)`,
    filters `.eq('is_published', true)`, orders `.order('updated_at', { ascending: false })`,
    applies `.limit(limit)`, throws on error, and flattens the `question_count` aggregate using
    the identical pattern as fetchMyQuizzes (`agg?.[0]?.count ?? 0`). Return cast `as unknown as Quiz[]`.
    Do NOT touch fetchPublishedQuizzes — QuizListPage depends on its created_at ordering and lean select
    (per RESEARCH anti-pattern "fetchPublishedQuizzes mutation").
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - src/5-entities/quiz/api.ts exports `fetchCarouselQuizzes`.
    - The function's select list includes `updated_at` and `question_count:questions(count)`.
    - The function orders by `updated_at` descending and applies `.limit(limit)` with default 12.
    - fetchPublishedQuizzes body is byte-for-byte unchanged.
    - `npx vue-tsc --noEmit` reports no new type errors.
  </acceptance_criteria>
  <done>fetchCarouselQuizzes exists, type-checks, returns published quizzes newest-first by updated_at with question_count populated; fetchPublishedQuizzes untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rebind "Все тесты" nav links to /quizzes</name>
  <read_first>
    - src/3-widgets/AppHeader.vue (the file being modified — identify the logo RouterLink vs the "Все тесты" nav RouterLink)
    - src/3-widgets/AppFooter.vue (the file being modified — the single "Все тесты" RouterLink)
    - .planning/phases/06-landing-page-service-overview-public-quiz-carousel-and-recen/06-PATTERNS.md (sections "src/3-widgets/AppHeader.vue" and "src/3-widgets/AppFooter.vue")
  </read_first>
  <action>
    In src/3-widgets/AppHeader.vue: change the `to="/"` on the nav "Все тесты" RouterLink to `to="/quizzes"`.
    Leave the logo/brand RouterLink `to="/"` unchanged — it correctly targets the landing page (D-01).
    In src/3-widgets/AppFooter.vue: change the `to="/"` on the "Все тесты" RouterLink to `to="/quizzes"`.
    Do not change any other attributes, classes, or RouterLink elements. Note: QuizResultPage.vue
    `to="/"` links are intentionally NOT changed (their copy "Перейти на Quiz Flow" means the landing
    page — per PATTERNS.md QuizResultPage section).
  </action>
  <verify>
    <automated>npx steiger src --reporter pretty</automated>
  </verify>
  <acceptance_criteria>
    - AppHeader.vue: exactly one RouterLink with `to="/quizzes"`; the logo RouterLink still has `to="/"`.
    - AppFooter.vue: the "Все тесты" RouterLink has `to="/quizzes"`.
    - No other `to="/"` occurrence in these two files was altered.
    - steiger reports no FSD layer violations.
  </acceptance_criteria>
  <done>Both "Все тесты" links route to /quizzes; logo link still routes to /; steiger clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase (anon role) | fetchCarouselQuizzes reads `quizzes` rows via the existing public anon RLS policy (migration 007) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure | fetchCarouselQuizzes select list | accept | Select list is explicit (`id, title, description, cover_url, time_limit_sec, updated_at, question_count`); no `is_correct`, `password_hash`, or owner-private columns. `is_published=true` filter restricts rows to the already-public set. |
| T-06-02 | Tampering | npm/pip/cargo installs | mitigate | No packages installed in this phase (RESEARCH Package Legitimacy Audit: none). No install task exists. |
</threat_model>

<verification>
- `npx vue-tsc --noEmit -p tsconfig.app.json` passes.
- `npx steiger src` reports no layer violations.
- Grep confirms `fetchCarouselQuizzes` exists in api.ts and `fetchPublishedQuizzes` still orders by `created_at`.
</verification>

<success_criteria>
- fetchCarouselQuizzes returns up to 12 published quizzes ordered by updated_at DESC with question_count.
- fetchPublishedQuizzes is unchanged.
- "Все тесты" links in AppHeader and AppFooter point to /quizzes; AppHeader logo still points to /.
</success_criteria>

<output>
Create `.planning/phases/06-landing-page-service-overview-public-quiz-carousel-and-recen/06-01-SUMMARY.md` when done
</output>
